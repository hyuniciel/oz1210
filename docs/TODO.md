b- [ ] `.cursor/` 디렉토리
  - [ ] `rules/` 커서룰
  - [ ] `mcp.json` MCP 서버 설정
  - [ ] `dir.md` 프로젝트 디렉토리 구조
- [ ] `.github/` 디렉토리
- [ ] `.husky/` 디렉토리
- [ ] `app/` 디렉토리
  - [ ] `favicon.ico` 파일
  - [ ] `not-found.tsx` 파일
  - [ ] `robots.ts` 파일
  - [ ] `sitemap.ts` 파일
  - [ ] `manifest.ts` 파일
- [ ] `supabase/` 디렉토리
- [ ] `public/` 디렉토리
  - [ ] `icons/` 디렉토리
  - [ ] `logo.png` 파일
  - [ ] `og-image.png` 파일
- [ ] `tsconfig.json` 파일
- [ ] `.cursorignore` 파일
- [ ] `.gitignore` 파일
- [ ] `.prettierignore` 파일
- [ ] `.prettierrc` 파일
- [ ] `tsconfig.json` 파일
- [ ] `eslint.config.mjs` 파일
- [ ] `AGENTS.md` 파일


# My Trip - 개발 TODO 리스트

> PRD, Flowchart, Design 문서 기반 작업 항목 정리

## Phase 1: 기본 구조 & 공통 설정

- [ ] 프로젝트 셋업
  - [ ] 환경변수 설정 (`.env`)
    - [ ] `NEXT_PUBLIC_TOUR_API_KEY` (한국관광공사 API)
    - [ ] `TOUR_API_KEY` (서버 사이드용)
    - [ ] `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` (네이버 지도)
    - [ ] Clerk 인증 키 확인
    - [ ] Supabase 키 확인
- [x] API 클라이언트 구현
  - [x] `lib/api/tour-api.ts` 생성
    - [x] `getAreaCode()` - 지역코드 조회 (`areaCode2`)
    - [x] `getAreaBasedList()` - 지역 기반 목록 (`areaBasedList2`)
    - [x] `searchKeyword()` - 키워드 검색 (`searchKeyword2`)
    - [x] `getDetailCommon()` - 공통 정보 (`detailCommon2`)
    - [x] `getDetailIntro()` - 소개 정보 (`detailIntro2`)
    - [x] `getDetailImage()` - 이미지 목록 (`detailImage2`)
    - [x] `getDetailPetTour()` - 반려동물 정보 (`detailPetTour2`)
    - [x] 공통 파라미터 처리 (serviceKey, MobileOS, MobileApp, _type)
    - [x] 에러 처리 및 재시도 로직
    - ---
    - [x] `getServiceKey()` 함수 구현 (환경변수에서 API 키 가져오기)
    - [x] `buildQueryParams()` 함수 구현 (공통 파라미터 + 커스텀 파라미터 병합)
    - [x] `handleApiError()` 함수 구현 (API 에러 처리 및 사용자 친화적 메시지 변환)
    - [x] `retryRequest()` 함수 구현 (재시도 로직: 최대 3회, 지수 백오프)
    - [x] `TourApiError` 커스텀 에러 클래스 구현
    - [x] `extractItems()` 유틸리티 함수 구현 (단일/배열 항목 처리)
    - [x] 타임아웃 처리 (10초)
    - [x] Rate Limit 처리 (429 에러)
- [x] 타입 정의
  - [x] `lib/types/tour.ts` 생성
    - [x] `TourItem` 인터페이스 (목록)
    - [x] `TourDetail` 인터페이스 (상세)
    - [x] `TourIntro` 인터페이스 (운영정보)
    - [x] `TourImage` 인터페이스 (이미지)
    - [x] `PetTourInfo` 인터페이스 (반려동물)
    - ---
    - [x] `AreaCode` 인터페이스 (지역코드 정보)
    - [x] `ApiResponseHeader` 인터페이스 (API 응답 헤더)
    - [x] `ApiListResponseBody<T>` 인터페이스 (목록 조회 응답 본문)
    - [x] `ApiDetailResponseBody<T>` 인터페이스 (상세 조회 응답 본문)
    - [x] `ApiListResponse<T>` 인터페이스 (목록 조회 전체 응답)
    - [x] `ApiDetailResponse<T>` 인터페이스 (상세 조회 전체 응답)
    - [x] `AreaBasedListResult` 인터페이스 (지역 기반 목록 조회 결과)
    - [x] `KeywordSearchResult` 인터페이스 (키워드 검색 결과)
  - [ ] `lib/types/stats.ts` 생성
    - [ ] `RegionStats` 인터페이스
    - [ ] `TypeStats` 인터페이스
    - [ ] `StatsSummary` 인터페이스
- [x] 레이아웃 구조
  - [x] `app/layout.tsx` 업데이트
    - [x] 메타데이터 설정
    - [x] 헤더/푸터 구조 확인
    - ---
    - [x] Open Graph 메타태그 추가
    - [x] Twitter Card 메타태그 추가
    - [x] Footer 컴포넌트 추가
    - [x] Toaster Provider 추가
    - [x] 반응형 레이아웃 구조 (flex min-h-screen flex-col)
  - [x] `components/Navbar.tsx` 업데이트
    - [x] 로고, 검색창, 로그인 버튼
    - [x] 네비게이션 링크 (홈, 통계, 북마크)
    - ---
    - [x] 로고 변경 ("SaaS Template" → "My Trip")
    - [x] 검색창 추가 (데스크톱: 인라인, 모바일: 메뉴 내)
    - [x] 네비게이션 링크 추가 (홈, 통계, 북마크)
    - [x] 모바일 햄버거 메뉴 구현
    - [x] 반응형 디자인 적용
    - [x] 검색 기능 연동 (URL 쿼리 파라미터)
  - [x] `components/Footer.tsx` 생성
    - [x] 저작권 정보 표시
    - [x] About, Contact 링크
    - [x] 한국관광공사 API 제공 표시
    - [x] 반응형 디자인
- [x] 공통 컴포넌트
  - [x] `components/ui/loading.tsx` - 로딩 스피너
    - ---
    - [x] 크기 변형 지원 (sm, md, lg)
    - [x] 색상 변형 지원 (primary, secondary, muted)
    - [x] 텍스트 옵션 지원
    - [x] 전체 화면 모드 지원
  - [x] `components/ui/skeleton.tsx` - 스켈레톤 UI
    - ---
    - [x] shadcn/ui Skeleton 컴포넌트 설치
  - [x] `components/ui/error.tsx` - 에러 메시지
    - ---
    - [x] 에러 타입별 아이콘 (network, server, client, unknown)
    - [x] 재시도 버튼 옵션
    - [x] Alert 컴포넌트 기반 구현
  - [x] `lib/utils/toast.ts` - 토스트 알림 유틸리티
    - ---
    - [x] toastSuccess, toastError, toastInfo, toastWarning 함수
    - [x] toastLoading 함수
    - [x] sonner 기반 구현
    - [x] Toaster Provider는 layout.tsx에 추가됨

## Phase 2: 홈페이지 (`/`) - 관광지 목록

- [x] 페이지 기본 구조
  - [x] `app/page.tsx` 생성
    - [x] 기본 레이아웃 (헤더, 메인, 푸터)
    - [x] 반응형 컨테이너 설정
    - ---
    - [x] Server Component로 구현 (Next.js 15 App Router)
    - [x] 반응형 컨테이너 설정 (max-w-7xl, mx-auto, px-4)
    - [x] 필터 섹션 영역 플레이스홀더 생성
    - [x] 관광지 목록 섹션 영역 플레이스홀더 생성
    - [x] 네이버 지도 섹션 영역 플레이스홀더 생성 (데스크톱: sticky, 모바일: 숨김)
    - [x] URL 쿼리 파라미터 처리 준비 (searchParams 타입 정의 및 읽기)
    - [x] 검색 키워드 표시 기능
    - [x] 반응형 레이아웃 구조 (데스크톱: 리스트 50% + 지도 50%, 모바일: 리스트만 표시)
- [x] 관광지 목록 기능 (MVP 2.1)
  - [x] `components/tour-card.tsx` 생성
    - [x] 썸네일 이미지 (기본 이미지 fallback)
    - [x] 관광지명
    - [x] 주소 표시
    - [x] 관광 타입 뱃지
    - [x] 간단한 개요 (1-2줄)
    - [x] 호버 효과 (scale, shadow)
    - [x] 클릭 시 상세페이지 이동
    - ---
    - [x] Next.js Image 컴포넌트 사용 (이미지 최적화)
    - [x] 이미지 비율 16:9 (aspect-video)
    - [x] 관광 타입별 뱃지 색상 구분
    - [x] HTML 태그 제거 및 텍스트 정리
    - [x] 전화번호 클릭 이벤트 처리
    - [x] 접근성 개선 (focus ring, aria-label)
  - [x] `components/tour-list.tsx` 생성
    - [x] 그리드 레이아웃 (반응형)
    - [x] 카드 목록 표시
    - [x] 로딩 상태 (Skeleton UI)
    - [x] 빈 상태 처리
    - ---
    - [x] 반응형 그리드 (모바일: 1열, 태블릿: 2열, 데스크톱: 3열)
    - [x] TourListSkeleton 컴포넌트 구현
    - [x] EmptyState 컴포넌트 구현
    - [x] 에러 상태 처리 (Error 컴포넌트 사용)
  - [x] API 연동
    - [x] `getAreaBasedList()` 호출
    - [x] 데이터 파싱 및 표시
    - [x] 에러 처리
    - ---
    - [x] Server Component에서 API 호출
    - [x] 키워드 검색 시 `searchKeyword()` 호출
    - [x] 기본값: 서울 지역 (areaCode: "1")
    - [x] URL 파라미터 기반 조건부 API 호출
    - [x] try-catch로 에러 처리
    - [x] TourList 컴포넌트에 데이터 전달
  - [x] 유틸리티 함수
    - [x] `lib/utils/tour.ts` 생성
    - [x] `getContentTypeName()` - 타입 ID를 타입명으로 변환
    - [x] `getContentTypeInfo()` - 타입명과 색상 정보 반환
    - [x] `formatAddress()` - 주소 포맷팅
    - [x] `truncateText()` - 텍스트 말줄임표 처리
    - [x] `stripHtmlTags()` - HTML 태그 제거
  - [x] 이미지 설정
    - [x] `next.config.ts`에 외부 이미지 도메인 추가 (apis.data.go.kr)
- [x] 필터 기능
  - [x] `components/tour-filters.tsx` 생성
    - [x] 지역 필터 (시/도 선택)
      - [x] `getAreaCode()` API로 지역 목록 로드
      - [x] 드롭다운 또는 버튼 그룹
      - [x] "전체" 옵션
    - [x] 관광 타입 필터
      - [x] 관광지(12), 문화시설(14), 축제/행사(15), 여행코스(25), 레포츠(28), 숙박(32), 쇼핑(38), 음식점(39)
      - [x] 다중 선택 가능
      - [x] "전체" 옵션
    - [x] 반려동물 동반 가능 필터 (MVP 2.5)
      - [x] 토글 버튼
      - [x] 크기별 필터 (소형, 중형, 대형)
      - ---
      - [x] 반려동물 크기별 필터 구현
        - [x] `components/tour-filters.tsx`에 크기 필터 UI 추가
          - [x] Checkbox 그룹으로 크기 선택 (소형, 중형, 대형)
          - [x] 반려동물 필터 활성화 시에만 크기 필터 표시
          - [x] URL 파라미터: `petSize` (다중 선택, 쉼표로 구분)
        - [x] 필터 상태 관리 구현
          - [x] `petSize` 파라미터 읽기 및 업데이트
          - [x] 반려동물 필터와 크기 필터 조합 처리
          - [x] 필터 변경 시 URL 파라미터 동기화
        - [x] 필터링 로직 구현
          - [x] `app/page.tsx`에서 `petSize` 파라미터 읽기
          - [x] 반려동물 정보 조회 후 크기 필터 적용
          - [x] `matchesPetSizeFilter()` 함수 활용
        - [x] UI/UX 개선
          - [x] 크기 필터 레이블 및 아이콘 추가
          - [x] 반려동물 필터 활성화 시 크기 필터 표시/숨김
          - [x] 크기 필터 초기화 기능
      - ---
      - 추가 개발 사항
        - [x] `components/tour-filters.tsx`에 PET_SIZES 상수 정의 및 크기 필터 UI 구현
        - [x] `handlePetSizeChange` 핸들러 함수 구현 (다중 선택 지원)
        - [x] `getActiveFilterCount` 함수에 크기 필터 카운트 추가
        - [x] `getActiveFilters` 함수에 크기 필터 정보 추가
        - [x] `handleRemoveFilter` 함수에 `petSize` 케이스 추가
        - [x] `handlePetFriendlyChange` 함수에서 반려동물 필터 해제 시 크기 필터도 함께 해제
        - [x] `app/page.tsx`에 `petSize` 파라미터 읽기 및 필터링 로직 추가
        - [x] `matchesPetSizeFilter` 함수 import 및 활용
        - [x] `HomePageProps` 인터페이스에 `petSize` 필드 추가
      - ---
      - [x] `lib/utils/pet.ts` 생성 (반려동물 정보 처리 유틸리티 함수)
        - [x] `isPetFriendly()` 함수 구현
        - [x] `getPetSizeLabel()` 함수 구현
        - [x] `getPetPlaceLabel()` 함수 구현
        - [x] `matchesPetSizeFilter()` 함수 구현
        - [x] `getPetInfoSummary()` 함수 구현
      - [x] shadcn/ui Switch 컴포넌트 설치
      - [x] `components/tour-filters.tsx`에 반려동물 필터 추가
        - [x] Switch 컴포넌트로 토글 구현
        - [x] Heart 아이콘 추가
        - [x] URL 파라미터 동기화 (`petFriendly`)
        - [x] 필터 상태 관리
      - [x] `app/page.tsx`에 반려동물 필터 로직 구현
        - [x] `petFriendly` 파라미터 읽기
        - [x] 필터 활성화 시 반려동물 정보 병렬 조회 (`getDetailPetTour()`)
        - [x] `Promise.allSettled()` 사용하여 에러 처리
        - [x] 반려동물 동반 가능한 관광지만 필터링
        - [x] 성능 최적화 (최대 20개 항목만 조회)
      - [x] `components/tour-card.tsx`에 반려동물 뱃지 추가
        - [x] `petInfo` prop 추가
        - [x] 반려동물 동반 가능 뱃지 표시 (🐾 아이콘)
        - [x] 크기 제한 정보 표시 (반응형)
        - [x] 이미지 위 오버레이로 배치
      - [x] `components/tour-list.tsx` 업데이트
        - [x] `petInfoMap` prop 추가
        - [x] TourCard에 petInfo 전달
      - [x] 성능 최적화 및 에러 처리
        - [x] 병렬 API 호출 최적화
        - [x] 일부 관광지 조회 실패 시에도 계속 진행
        - [x] 콘솔 로그 추가 (디버깅용)
    - [x] 정렬 옵션
      - [x] 최신순 (modifiedtime)
      - [x] 이름순 (가나다)
    - [x] 필터 상태 관리 (URL 쿼리 파라미터 또는 상태)
    - ---
    - [x] Client Component로 구현 (필터 인터랙션 처리)
    - [x] 지역 필터: Select 컴포넌트 사용 (shadcn/ui)
      - [x] 지역 목록을 props로 받거나 내부에서 로드
      - [x] "전체" 옵션 추가 (value: "" 또는 "all")
      - [x] 현재 선택된 지역 표시
      - [x] 지역 변경 시 URL 파라미터 업데이트
    - [x] 관광 타입 필터: Checkbox 그룹 사용
      - [x] 관광 타입 상수 정의 (CONTENT_TYPES 배열)
      - [x] Checkbox 컴포넌트로 다중 선택 UI 구현
      - [x] "전체 선택" 체크박스 추가 (모든 타입 선택/해제)
      - [ ] 선택된 타입 개수 표시 (선택 사항)
      - [x] 첫 번째 선택된 타입만 URL 파라미터에 반영
    - [x] 정렬 옵션: Select 컴포넌트 사용
      - [x] 정렬 옵션 상수 정의 ("latest", "name")
      - [x] 기본값: "latest" (최신순)
      - [x] 정렬 변경 시 URL 파라미터 업데이트
    - [x] useRouter와 useSearchParams로 URL 쿼리 파라미터 관리
      - [x] Next.js 15의 useSearchParams 사용 (Suspense 필요할 수 있음)
      - [x] useRouter로 URL 업데이트
      - [x] 필터 변경 시 기존 파라미터 유지하면서 업데이트
    - [x] 필터 변경 시 router.push()로 URL 업데이트
      - [x] 필터 변경 핸들러 구현
      - [x] URLSearchParams로 쿼리 파라미터 구성
      - [x] pageNo를 1로 리셋
      - [x] keyword 파라미터 유지 (검색과 필터 조합)
    - [x] 필터 초기값을 URL 파라미터에서 읽기
      - [x] 컴포넌트 마운트 시 URL 파라미터 읽기
      - [x] 지역 필터 초기값: areaCode || "1" (서울)
      - [x] 관광 타입 필터 초기값: contentTypeId (없으면 전체)
      - [x] 정렬 초기값: sort || "latest"
    - [x] 필터 리셋 버튼 (모든 필터 초기화)
      - [x] 리셋 버튼 UI 추가
      - [x] 리셋 핸들러 구현 (기본값으로 초기화)
      - [x] URL 파라미터 제거 또는 기본값으로 설정
    - [x] 반응형 디자인 (모바일: 세로 배치, 데스크톱: 가로 배치)
      - [x] 모바일: flex-col, gap-4
      - [x] 데스크톱: flex-row, gap-6
      - [x] 필터 레이블 및 컨트롤 크기 조정
    - [x] 필터 섹션 접기/펼치기 기능 (모바일)
      - [x] Collapsible 컴포넌트 사용 (shadcn/ui 설치 필요)
      - [x] 기본 상태: 접힘 (모바일), 펼침 (데스크톱)
      - [x] 접기/펼치기 토글 버튼
      - ---
      - [x] 모바일 필터 접기/펼치기 기능 구현
        - [x] shadcn/ui Collapsible 컴포넌트 설치
          - [x] `pnpx shadcn@latest add collapsible` 명령어 실행
          - [x] `components/ui/collapsible.tsx` 파일 생성 확인
          - [x] Collapsible 컴포넌트 import 경로 확인
        - [x] `components/tour-filters.tsx`에 Collapsible 적용
          - [x] 전체 필터 컨테이너를 Collapsible로 감싸기
          - [x] CollapsibleTrigger: 토글 버튼 (모바일에서만 표시)
          - [x] CollapsibleContent: 필터 컨텐츠 (접기/펼치기 대상)
          - [x] 모바일에서만 Collapsible 적용 (데스크톱은 항상 펼침)
          - [x] 기본 상태: 모바일에서 접힘, 데스크톱에서 펼침
        - [x] 상태 관리 구현
          - [x] `useState`로 `isOpen`, `isMobile` 상태 추가
          - [x] 기본값: `false` (모바일에서 접힘)
          - [x] 모바일/데스크톱 감지 로직 구현
            - [x] `useEffect`와 `window.matchMedia` 사용
            - [x] 브레이크포인트: 768px (md 브레이크포인트)
            - [x] 데스크톱 감지 시 `isOpen`을 `true`로 설정
            - [x] 리사이즈 이벤트 리스너 추가
            - [x] 컴포넌트 언마운트 시 리스너 제거
          - [x] 접기/펼치기 상태를 로컬 상태로 관리 (URL 파라미터 불필요)
        - [x] 토글 버튼 UI 구현
          - [x] 토글 버튼 디자인 및 레이아웃
            - [x] 버튼 위치: 필터 섹션 상단 (모바일에서만 표시)
            - [x] 버튼 스타일: 전체 너비, 배경색, 테두리, 패딩
            - [x] 왼쪽: Filter 아이콘 + "필터" 텍스트
            - [x] 오른쪽: ChevronDown 아이콘 (회전 애니메이션)
          - [x] 아이콘 추가 (ChevronDown, Filter from lucide-react)
            - [x] 접힘 상태: 아래 방향 화살표
            - [x] 펼침 상태: 위 방향 화살표 (회전)
          - [x] 접근성 속성 추가
            - [x] `aria-label`: "필터 접기/펼치기" (동적)
            - [x] `aria-expanded`: 접기/펼치기 상태
            - [x] 키보드 네비게이션 지원 (Enter, Space - Collapsible 자동 지원)
        - [x] 애니메이션 및 전환 효과
          - [x] Collapsible 기본 애니메이션 활용
          - [x] 아이콘 회전 애니메이션 추가 (`transform: rotate(180deg)`)
          - [x] 전환 시간 조정 (300ms, `duration-300`)
        - [x] 활성 필터 개수 표시 (선택 사항)
          - [x] 활성 필터 계산 로직 구현 (`getActiveFilterCount()`)
            - [x] 지역 필터: 기본값("1")과 다르면 카운트
            - [x] 관광 타입 필터: 선택되면 카운트
            - [x] 정렬 필터: 기본값("latest")과 다르면 카운트
            - [x] 반려동물 필터: 활성화되면 카운트
          - [x] 토글 버튼에 뱃지 표시 (예: "필터 (3)")
          - [x] 활성 필터가 없으면 뱃지 숨김
        - [x] 반응형 동작 구현
          - [x] 데스크톱 동작 (화면 너비 >= 768px)
            - [x] 항상 펼쳐진 상태 유지
            - [x] 토글 버튼 숨김 (`md:hidden`)
            - [x] Collapsible 기능 비활성화 (조건부 렌더링)
          - [x] 모바일 동작 (화면 너비 < 768px)
            - [x] 기본 상태: 접힘
            - [x] 토글 버튼 표시
            - [x] Collapsible 기능 활성화
          - [x] 화면 크기 변경 감지
            - [x] `window.matchMedia` 리스너 추가
            - [x] 화면 크기 변경 시 상태 업데이트
        - [x] 테스트 및 검증
          - [x] 모바일 환경에서 접기/펼치기 테스트
          - [x] 데스크톱 환경에서 항상 펼침 확인
          - [x] 화면 크기 변경 시 동작 확인
          - [x] 접근성 테스트 (키보드 네비게이션, 스크린 리더)
  - [x] 필터 적용 로직
    - [x] 필터 변경 시 API 재호출
    - [x] 필터 조합 처리
    - ---
    - [x] URL 쿼리 파라미터 기반 필터 상태 동기화
      - [x] areaCode 파라미터 처리 (지역 필터)
        - [x] 기본값: "1" (서울)
        - [x] "전체" 선택 시 파라미터 제거 또는 빈 값
      - [x] contentTypeId 파라미터 처리 (단일 선택, 첫 번째 값만 사용)
        - [x] 관광 타입 필터에서 첫 번째 선택된 값만 사용
        - [x] "전체" 선택 시 파라미터 제거
      - [x] sort 파라미터 처리 (정렬 옵션)
        - [x] "latest": 최신순 (modifiedtime DESC)
        - [x] "name": 이름순 (title ASC)
        - [x] 기본값: "latest"
    - [x] 필터 변경 시 pageNo를 1로 리셋
      - [x] 필터 변경 핸들러에서 page 파라미터 제거 또는 1로 설정
    - [x] 필터 변경 시 로딩 상태 표시
      - [x] 필터 변경 시 로딩 인디케이터 표시 (선택 사항)
      - [x] 또는 전체 페이지 리로드로 자연스러운 로딩
    - [x] 필터 조합 시 AND 조건 처리
      - [x] 지역 AND 관광 타입 AND 정렬
      - [x] 모든 필터가 동시에 적용되도록 API 호출
    - [x] app/page.tsx에서 필터 파라미터 읽기 및 API 호출에 반영
      - [x] searchParams에서 areaCode, contentTypeId, sort 읽기
      - [x] getAreaBasedList() 호출 시 필터 파라미터 전달
      - [x] searchKeyword() 호출 시에도 필터 파라미터 전달
      - [x] 정렬은 클라이언트 사이드에서 처리 (API가 정렬을 지원하지 않을 경우)
  - [x] 필터 전용 로직
    - [x] `lib/utils/filters.ts` 생성 (필터 전용 유틸리티 함수)
      - [x] FilterParams 인터페이스 정의
      - [x] DEFAULT_FILTERS 상수 정의
      - [x] `parseFilterParams()` 함수 구현 (URL 파라미터 파싱)
      - [x] `buildFilterParams()` 함수 구현 (FilterParams → URLSearchParams)
      - [x] `mergeFilterParams()` 함수 구현 (필터 파라미터 병합)
      - [x] `resetFilterParams()` 함수 구현 (필터 리셋)
      - [x] `updateFilterParams()` 함수 구현 (필터 파라미터 업데이트)
      - [x] `buildFilterQueryString()` 함수 구현 (쿼리 문자열 생성)
    - [x] `components/tour-filters.tsx` 리팩토링
      - [x] 필터 유틸리티 함수 import 및 사용
      - [x] `parseFilterParams()` 사용하여 필터 파라미터 읽기
      - [x] `updateFilterParams()` 사용하여 URL 파라미터 업데이트
      - [x] `resetFilterParams()` 사용하여 필터 리셋
      - [x] 필터 변경 핸들러들 필터 유틸리티 함수 사용하도록 수정
    - [x] `app/page.tsx` 리팩토링
      - [x] `parseFilterParams()` 사용하여 필터 파라미터 읽기
      - [x] 필터 기본값을 DEFAULT_FILTERS 상수 사용
    - ---
    - 추가 개발 사항
      - [x] `lib/utils/filters.ts` 파일 생성 및 필터 유틸리티 함수 구현
      - [x] FilterParams 인터페이스로 필터 파라미터 타입 정의
      - [x] DEFAULT_FILTERS 상수로 필터 기본값 중앙 관리
      - [x] `parseFilterParams()` 함수로 URL 파라미터 파싱 로직 통합
      - [x] `buildFilterParams()` 함수로 FilterParams를 URLSearchParams로 변환
      - [x] `mergeFilterParams()` 함수로 필터 파라미터 병합 로직 구현
      - [x] `resetFilterParams()` 함수로 필터 리셋 로직 구현 (keyword 유지)
      - [x] `updateFilterParams()` 함수로 필터 파라미터 업데이트 로직 구현
      - [x] `components/tour-filters.tsx`에서 필터 유틸리티 함수 사용하도록 리팩토링
      - [x] `app/page.tsx`에서 필터 유틸리티 함수 사용하도록 리팩토링
      - [x] 기존 기능 정상 작동 확인 및 타입 안정성 검증
    - ---
  - [x] 지역 목록 로드
    - [x] Server Component에서 지역 목록 미리 로드
      - [x] app/page.tsx에서 getAreaCode() 호출
      - [x] 지역 목록을 TourFilters 컴포넌트에 props로 전달
    - [x] 또는 Client Component에서 로드
      - [x] TourFilters 내부에서 useEffect로 getAreaCode() 호출
      - [x] 로딩 상태 처리
      - [x] 에러 처리
      - ---
      - 추가 개발 사항
        - [x] `lib/constants/areas.ts` 생성 (기본 지역 목록 상수 정의)
        - [x] `TourFiltersProps` 인터페이스에서 `areas` prop을 optional로 변경
        - [x] 지역 목록 로드 상태 관리 (useState: areasState, isLoadingAreas, areasError)
        - [x] useEffect로 지역 목록 로드 로직 구현 (areas prop이 없을 때만)
        - [x] 로딩 상태 UI 구현 (Skeleton 컴포넌트 사용)
        - [x] 에러 처리 UI 구현 (에러 메시지 표시 및 기본 지역 목록 사용)
        - [x] 하위 호환성 유지 (areas prop이 있으면 Client Component에서 로드하지 않음)
        - [x] 기존 기능 정상 작동 확인 (areas prop 전달 시)
        - [x] Client Component 로드 기능 정상 작동 확인
      - ---
  - [x] 관광 타입 상수 정의
    - [x] `lib/constants/content-types.ts` 생성
      - [x] CONTENT_TYPES 배열 정의
      - [x] 각 타입의 id, name, label 포함
      - [x] getContentTypeName() 함수와 일치하도록 정의
  - [x] 필터 컴포넌트 통합
    - [x] `app/page.tsx`에 TourFilters 컴포넌트 배치
      - [x] 필터 섹션 영역에 컴포넌트 추가
      - [x] 지역 목록을 Server Component에서 로드하여 props로 전달
      - [x] Suspense로 감싸서 로딩 상태 처리 (선택 사항)
    - [x] 필터와 검색 기능 연동
      - [x] 검색 키워드와 필터 동시 적용 가능하도록 처리
      - [x] 필터 변경 시 검색 키워드 유지
  - [x] 클라이언트 사이드 정렬 구현 (API가 정렬을 지원하지 않을 경우)
    - [x] `lib/utils/sort.ts` 생성
      - [x] `sortToursByLatest()` 함수 (modifiedtime DESC)
      - [x] `sortToursByName()` 함수 (title ASC)
    - [x] TourList 컴포넌트에서 정렬 적용
      - [x] sort 파라미터에 따라 정렬 함수 선택
      - [x] 정렬된 목록 표시
  - [x] 필터 UI/UX 개선
    - [x] 필터 레이블 및 아이콘 추가
      - [x] 지역 필터: MapPin 아이콘
      - [x] 관광 타입 필터: Tag 아이콘
      - [x] 정렬: ArrowUpDown 아이콘
    - [x] 필터 적용된 상태 표시
      - [x] 선택된 필터 개수 표시 (선택 사항)
      - [x] 활성 필터 뱃지 표시 (선택 사항)
      - ---
      - 추가 개발 사항
        - [x] 관광 타입 필터 활성 상태 강조 스타일 추가 (`border-primary bg-primary/5`)
        - [x] 선택된 타입 개수 표시 추가 (관광 타입 레이블 옆에 "(N)" 형식)
        - [x] 모든 필터 컨트롤의 활성 상태 강조 일관성 확인
        - [x] 활성 필터 뱃지 표시 및 제거 기능 정상 작동 확인
        - [x] 활성 필터 개수 표시 확인 (모바일 토글 버튼, 데스크톱 요약)
      - ---
    - [x] 접근성 개선
      - [x] ARIA 라벨 추가
      - [x] 키보드 네비게이션 지원
      - [x] 포커스 관리
  - [x] 에러 처리
    - [x] 지역 목록 로드 실패 시 처리
      - [x] 에러 메시지 표시
      - [x] 기본 지역 목록 사용 (하드코딩)
    - [x] 필터 변경 실패 시 처리
      - [x] URL 업데이트 실패 시 에러 감지
      - [x] 사용자에게 알림
      - ---
      - 추가 개발 사항
        - [x] `app/page.tsx`에서 지역 목록 로드 실패 시 `DEFAULT_AREAS` 사용
        - [x] `components/tour-filters.tsx`에 필터 변경 에러 상태 관리 추가 (`filterError`)
        - [x] `updateParams` 함수를 try-catch로 감싸서 에러 처리
        - [x] 필터 변경 실패 시 Alert 컴포넌트로 에러 메시지 표시
        - [x] 필터 변경 실패 시 Toast 알림 표시 (`toastError` 사용)
        - [x] 에러 발생 시 사용자 친화적인 메시지 제공
        - [x] 기존 기능 정상 작동 확인
      - ---
- [x] 검색 기능 (MVP 2.3)
  - [x] 검색창 UI (Navbar에 구현됨)
    - [x] 검색창 UI (헤더에 고정)
    - [x] 검색 아이콘
    - [x] 엔터 또는 버튼 클릭으로 검색
    - [x] 모바일/데스크톱 반응형 검색창
  - [x] 검색 API 연동
    - [x] `searchKeyword()` 호출
    - [x] 검색 결과 표시
    - [x] 검색 결과 개수 표시
    - [x] 결과 없음 메시지
  - [x] 검색 + 필터 조합
    - [x] 키워드 + 지역 필터
    - [x] 키워드 + 타입 필터
    - [x] 모든 필터 동시 적용
    - ---
    - 추가 개발 사항
      - [x] 검색 결과 개수 표시 추가 (`app/page.tsx`에서 "{keyword}" 검색 결과: N개 형식)
      - [x] 검색창 URL 파라미터 동기화 (`components/Navbar.tsx`에서 `useSearchParams` 사용)
      - [x] 검색 후 검색 키워드가 검색창에 유지되도록 처리
      - [x] 검색 결과 없음 메시지에 검색 키워드 포함 확인 (이미 구현됨)
      - [x] 검색 + 필터 조합 정상 작동 확인
      - [x] 기존 기능 정상 작동 확인
    - ---
- [x] 네이버 지도 연동 (MVP 2.2)
  - [x] `components/naver-map.tsx` 생성
    - [x] Naver Maps API v3 초기화
    - [x] 지도 컨테이너 설정
    - [x] 초기 중심 좌표 설정
    - [x] 줌 레벨 설정
  - [x] 마커 표시
    - [x] 관광지 목록을 마커로 표시
    - [x] 좌표 변환 (KATEC → WGS84: mapx/mapy / 10000000)
    - [x] 마커 클릭 시 인포윈도우
      - [x] 관광지명
      - [x] 간단한 설명
      - [x] "상세보기" 버튼
    - [ ] 관광 타입별 마커 색상 구분 (선택 사항)
  - [x] 지도-리스트 연동
    - [x] 선택된 관광지 ID로 지도 이동 및 마커 강조
    - [ ] 리스트 항목 호버 → 마커 강조 (선택 사항)
    - [x] 마커 클릭 → 리스트 항목 강조
  - [x] 지도 컨트롤
    - [x] 네이버 지도 기본 줌 컨트롤 사용
    - [x] 지도 유형 선택 (일반/위성 토글 버튼)
    - [ ] 현재 위치 버튼 (선택 사항)
  - [x] 반응형 레이아웃
    - [x] 데스크톱: 리스트(좌측 50%) + 지도(우측 50%) 분할
    - [x] 모바일: 탭 형태로 리스트/지도 전환
    - ---
    - 추가 개발 사항
      - [x] `lib/utils/map.ts` 생성 (좌표 변환 유틸리티 함수)
        - [x] `convertKATECToWGS84()` 함수 구현
        - [x] `calculateCenter()` 함수 구현 (관광지 목록 중심 좌표 계산)
        - [x] `calculateBounds()` 함수 구현 (지도 범위 자동 조정용)
      - [x] `components/naver-map.tsx` 생성 및 구현
        - [x] Naver Maps API 스크립트 동적 로드
        - [x] 지도 초기화 및 컨테이너 설정
        - [x] 관광지 목록 중심 좌표로 지도 초기화
        - [x] 모든 마커가 보이도록 지도 범위 자동 조정 (`fitBounds`)
        - [x] 마커 생성 및 표시
        - [x] 마커 클릭 시 인포윈도우 표시 (관광지명, 주소, 상세보기 버튼)
        - [x] 선택된 관광지로 지도 이동 및 인포윈도우 자동 열기
        - [x] 지도 타입 토글 버튼 (일반/위성)
        - [x] 로딩 상태 및 에러 처리
        - [x] Naver Maps API 타입 선언
      - [x] `components/tour-list-with-map.tsx` 생성 (반응형 레이아웃 컴포넌트)
        - [x] 모바일 탭 전환 구현 (Tabs 컴포넌트 사용)
        - [x] 데스크톱 분할 레이아웃 구현
        - [x] 지도-리스트 연동 상태 관리 (selectedTourId)
        - [x] 마커 클릭 시 선택된 관광지 ID를 URL에 추가
      - [x] `components/tour-list.tsx` 수정
        - [x] `selectedTourId` prop 추가
        - [x] 선택된 관광지 카드에 강조 스타일 적용
      - [x] `components/tour-card.tsx` 수정
        - [x] `isSelected` prop 추가
        - [x] 선택된 상태일 때 강조 스타일 적용 (ring, shadow)
      - [x] `app/page.tsx` 수정
        - [x] NaverMap 컴포넌트 지연 로딩 (`dynamic` import)
        - [x] TourListWithMap 컴포넌트 사용
        - [x] `selectedTourId` 파라미터 읽기
      - [x] 환경변수 지원 (`NEXT_PUBLIC_NAVER_MAP_NCP_KEY_ID` 또는 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID`)
      - [x] 성능 최적화 (지도 지연 로딩, 마커 최적화)
    - ---
- [ ] 페이지네이션
  - [x] 무한 스크롤 구현
    - [x] Intersection Observer 사용
    - [x] 하단 로딩 인디케이터
    - [x] 페이지당 10-20개 항목
  - [ ] 또는 페이지 번호 선택 방식
  - ---
  - 추가 개발 사항
    - [x] Server Action 생성
      - [x] `actions/load-tours.ts` 파일 생성
        - [x] `loadMoreTours` Server Action 함수 구현
          - [x] 파라미터 타입 정의 (`LoadMoreToursParams` 인터페이스)
          - [x] `pageNo`, `areaCode`, `contentTypeId`, `keyword`, `petFriendly`, `petSize`, `sort` 파라미터 처리
          - [x] `keyword`가 있으면 `searchKeyword` 호출, 없으면 `getAreaBasedList` 호출
          - [x] 반려동물 필터 활성화 시 `getDetailPetTour` 병렬 호출 (`Promise.allSettled` 사용)
          - [x] 반려동물 필터링 로직 적용 (`isPetFriendly`, `matchesPetSizeFilter` 사용)
          - [x] 클라이언트 사이드 정렬 적용 (`sortTours` 사용)
          - [x] `totalCount` 기반 `hasMore` 계산 (`currentPage * numOfRows < totalCount`)
          - [x] 반환 타입 정의 (`LoadMoreToursResult` 인터페이스)
          - [x] 에러 처리 및 로깅
    - [x] 페이지네이션 Hook 생성
      - [x] `hooks/use-infinite-tours.ts` 파일 생성
        - [x] `useInfiniteTours` Hook 구현
          - [x] 초기 데이터 props: `initialTours`, `initialTotalCount`, `initialPetInfoMap`
          - [x] 상태 관리 (`useState`):
            - [x] `allTours`: 누적된 모든 관광지 목록
            - [x] `petInfoMap`: 반려동물 정보 Map
            - [x] `currentPage`: 현재 페이지 번호 (초기값: 1)
            - [x] `hasMore`: 더 불러올 데이터가 있는지 여부
            - [x] `isLoading`: 로딩 중 여부
            - [x] `error`: 에러 상태
          - [x] `loadMore` 함수 구현:
            - [x] `isLoading` 또는 `!hasMore`일 때 early return
            - [x] `loadMoreTours` Server Action 호출
            - [x] 성공 시 `allTours`에 새 항목 추가, `petInfoMap` 업데이트
            - [x] `currentPage` 증가, `hasMore` 업데이트
            - [x] 에러 처리 및 상태 업데이트
          - [x] `reset` 함수 구현:
            - [x] 새로운 초기 데이터로 상태 초기화
            - [x] 필터/검색 변경 시 호출
          - [x] 반환값: `{ tours, petInfoMap, loadMore, reset, isLoading, hasMore, error }`
    - [x] Intersection Observer 컴포넌트 생성
      - [x] `components/infinite-scroll-trigger.tsx` 파일 생성
        - [x] `InfiniteScrollTrigger` 컴포넌트 구현
          - [x] Props: `onLoadMore`, `hasMore`, `isLoading`
          - [x] `useRef`로 감지 요소 참조 생성
          - [x] `useEffect`로 Intersection Observer 설정:
            - [x] `hasMore`가 false이거나 `isLoading`이 true일 때는 옵저버 비활성화
            - [x] 뷰포트 진입 시 `onLoadMore` 호출
            - [x] 컴포넌트 언마운트 시 옵저버 정리
          - [x] 감지 요소 렌더링 (하단에 배치될 빈 div 또는 로딩 인디케이터)
          - [x] 접근성 속성 추가 (`aria-label` 등)
          - [x] "더 이상 불러올 데이터가 없습니다" 메시지 표시
    - [x] TourList 컴포넌트 수정
      - [x] `components/tour-list.tsx` 수정
        - [x] `InfiniteScrollTrigger` import 추가
        - [x] Props에 `onLoadMore`, `hasMore`, `isLoadingMore` 추가
        - [x] 목록 하단에 `InfiniteScrollTrigger` 배치
        - [x] `isLoadingMore`일 때 하단 로딩 인디케이터 표시 (선택 사항)
    - [x] TourListWithMap 컴포넌트 수정
      - [x] `components/tour-list-with-map.tsx` 수정
        - [x] `useInfiniteTours` Hook import 및 사용
        - [x] Props에 `initialTotalCount` 추가
        - [x] 초기 데이터를 Hook에 전달하여 초기화
        - [x] `useEffect`로 필터/검색 변경 감지:
          - [x] `areaCode`, `contentTypeId`, `keyword`, `petFriendly`, `petSize`, `sort` 변경 감지
          - [x] 변경 시 `reset` 호출
        - [x] `loadMore` 함수를 `TourList`에 전달
        - [x] `hasMore`, `isLoading` 상태를 `TourList`에 전달
        - [x] 에러 처리 (에러 발생 시 `Error` 컴포넌트 표시)
    - [x] app/page.tsx 수정
      - [x] `app/page.tsx` 수정
        - [x] `totalCount` 정보 추출:
          - [x] `getAreaBasedList` 호출 시 `listResult.totalCount` 저장
          - [x] `searchKeyword` 호출 시 `searchResult.totalCount` 저장 (없으면 `tours.length` 사용)
        - [x] `TourListWithMap`에 `initialTotalCount` prop 추가
        - [x] 첫 페이지 데이터만 로드하는 로직 유지 (기존 로직 그대로)
    - [x] UI/UX 개선
      - [x] 하단 로딩 인디케이터 개선
        - [x] `InfiniteScrollTrigger` 내부 또는 `TourList` 하단에 로딩 스피너 표시
        - [x] `Loading` 컴포넌트 또는 Skeleton UI 사용
      - [x] "더 이상 불러올 데이터가 없습니다" 메시지 표시
        - [x] `hasMore`가 false이고 `allTours.length > 0`일 때 표시
        - [x] 적절한 스타일링 및 아이콘 추가
      - [x] 에러 발생 시 재시도 버튼 제공
        - [x] `TourList` 또는 `TourListWithMap`에 재시도 버튼 추가
        - [x] 클릭 시 `loadMore` 재호출
    - [x] 필터 변경 시 페이지 리셋 확인
      - [x] `components/tour-filters.tsx` 확인
        - [x] 필터 변경 시 `page` 파라미터 제거 또는 1로 설정하는지 확인 (이미 구현되어 있을 수 있음)
        - [x] `resetFilterParams` 함수에서 `page` 파라미터 제거 확인
    - [x] 성능 최적화
      - [x] 페이지당 항목 수 확인 (현재 20개 유지)
      - [x] 반려동물 필터 활성화 시에도 최대 20개만 조회하는지 확인
      - [x] 병렬 API 호출 최적화 확인 (`Promise.allSettled` 사용)
    - [x] 접근성 개선
      - [x] 키보드 네비게이션 지원 확인
      - [x] 스크린 리더를 위한 로딩 상태 알림 (`aria-live` 등)
      - [x] ARIA 라벨 추가 (`InfiniteScrollTrigger`에 `aria-label` 추가)
- [x] 최종 통합 및 스타일링
  - [ ] 모든 기능 통합 테스트
  - [x] 반응형 디자인 확인 (모바일/태블릿/데스크톱)
  - [x] 로딩 상태 개선
  - [x] 에러 처리 개선
  - ---
  - 추가 개발 사항
    - [x] 콘솔 로그 정리
      - [x] 개발용 `console.log` 조건부 처리 (`process.env.NODE_ENV === 'development'`)
      - [x] `app/page.tsx`의 반려동물 필터 로그 조건부 처리
      - [x] `actions/load-tours.ts`의 로그 조건부 처리
      - [x] `components/naver-map.tsx`의 `console.warn` 조건부 처리
      - [x] `console.error`는 유지 (에러 추적용)
    - [x] 로딩 상태 개선
      - [x] 스켈레톤 애니메이션 추가 (`animate-pulse` 클래스 추가)
      - [x] 무한 스크롤 로딩 인디케이터 개선 (이미 구현됨)
      - [x] 지도 로딩 상태 확인 (이미 구현됨)
    - [x] 에러 처리 개선
      - [x] `lib/utils/error.ts` 생성 (에러 처리 유틸리티 함수)
        - [x] `isOffline()` 함수 구현 (오프라인 상태 감지)
        - [x] `isNetworkError()` 함수 구현 (네트워크 에러 감지)
        - [x] `isTimeoutError()` 함수 구현 (타임아웃 에러 감지)
        - [x] `isServerError()` 함수 구현 (서버 에러 감지)
        - [x] `getErrorInfo()` 함수 구현 (에러 정보 추출 및 사용자 친화적 메시지 생성)
      - [x] `app/page.tsx`에 에러 처리 유틸리티 적용
      - [x] `hooks/use-infinite-tours.ts`에 에러 처리 유틸리티 적용
      - [x] 사용자 친화적 에러 메시지 제공
    - [x] 접근성 개선
      - [x] `components/tour-card.tsx`에 ARIA 속성 추가
        - [x] `aria-label` 추가 (관광지명, 주소, 반려동물 정보 포함)
        - [x] `aria-describedby` 추가 (관광지명과 연결)
      - [x] `components/tour-list.tsx`에 ARIA 속성 추가
        - [x] `role="list"` 및 `aria-label` 추가
        - [x] 각 항목에 `role="listitem"` 추가
      - [x] `components/tour-list-with-map.tsx`에 ARIA 속성 추가
        - [x] 탭 리스트에 `aria-label` 추가
        - [x] 각 탭 트리거에 `aria-label` 추가
      - [x] 키보드 네비게이션 확인 (이미 구현됨: focus ring, Tab 순서)
    - [x] 스타일 일관성 확인
      - [x] 색상 일관성 확인 (Primary 색상, 다크 모드 지원)
      - [x] 간격 일관성 확인 (gap-4, gap-6 사용)
      - [x] 타이포그래피 일관성 확인 (text-lg, text-sm 등)
    - [ ] 모든 기능 통합 테스트 (수동 테스트 필요)
      - ---
      - 추가 개발 사항
        - [x] 테스트 환경 준비
          - [x] 브라우저 준비: Chrome, Firefox, Safari, Edge (최신 버전)
          - [x] 디바이스 준비: 데스크톱 (1920x1080), 태블릿 (768x1024), 모바일 (375x667)
          - [x] 네트워크 환경 준비: 정상 연결, Slow 3G (타임아웃 테스트), Offline (에러 처리 테스트)
          - [x] 개발자 도구 준비: Network 탭, Console 탭 활성화
          - [x] 테스트 환경 준비 가이드 작성
            - [x] 브라우저 설치 및 확인 방법 문서화
            - [x] 디바이스 모드 설정 방법 문서화 (브라우저 개발자 도구)
            - [x] 네트워크 환경 시뮬레이션 방법 문서화 (Chrome DevTools)
            - [x] 개발자 도구 사용 방법 문서화 (Network 탭, Console 탭)
        - [x] 테스트 결과 기록 형식 정의
          - [x] 테스트 날짜 및 시간 기록 형식 정의: `YYYY-MM-DD HH:mm` 형식 사용
          - [x] 테스트 환경 기록 형식 정의: 브라우저 버전, 디바이스 해상도, 네트워크 상태를 명시
          - [x] 테스트 결과 기록 형식 정의: 통과(✓), 실패(✗), 부분 통과(△), 미실행(-) 사용
          - [x] 발견된 버그 또는 이슈 기록 형식 정의: 버그 ID, 제목, 설명, 재현 단계, 우선순위, 스크린샷 경로 포함
          - [x] 스크린샷 또는 로그 저장 위치 정의: `docs/test-results/screenshots/`, `docs/test-results/logs/` 디렉토리 사용
          - [x] 테스트 결과 기록 템플릿 작성 (`docs/test-results/template.md`)
        - [x] 테스트 완료 기준 정의
          - [x] 모든 기본 시나리오 테스트 통과 기준 정의: 100% 통과 필수
          - [x] 모든 복합 시나리오 테스트 통과 기준 정의: 95% 이상 통과 필수
          - [x] 모든 엣지 케이스 테스트 통과 기준 정의: 90% 이상 통과 필수 (Critical 버그 제외)
          - [x] 발견된 버그 수정 및 재테스트 완료 기준 정의: Critical 및 High 우선순위 버그는 모두 수정 후 재테스트 완료 필수
          - [x] 테스트 결과 문서화 완료 기준 정의: 모든 테스트 결과가 문서화되고 테스트 완료 보고서 작성 완료
          - [x] 테스트 완료 기준 문서 작성 (`docs/test-results/completion-criteria.md`)
      - [ ] 필터 + 검색 조합 테스트
        - ---
        - 추가 개발 사항
          - [ ] 테스트 전제 조건 확인
            - [ ] 검색창이 Navbar에 정상적으로 표시되는지 확인 (데스크톱 및 모바일)
            - [ ] 검색창에 키워드 입력 및 엔터 키로 검색 실행 가능한지 확인
            - [ ] 필터 컴포넌트가 정상적으로 표시되는지 확인
            - [ ] URL 파라미터가 정상적으로 읽히는지 확인 (`useSearchParams` 동작 확인)
          - [ ] 기본 시나리오 테스트 계획
            - [ ] 지역 필터 + 검색 조합 테스트 계획 수립
              - [ ] 테스트 목적: 지역 필터와 검색 기능이 함께 정상 작동하는지 확인
              - [ ] 테스트 단계 정의: 1) 지역 필터 선택, 2) 검색 실행, 3) 결과 확인
              - [ ] 확인 사항 정의: 검색 결과 필터링, URL 파라미터, 결과 개수 표시
            - [ ] 관광 타입 필터 + 검색 조합 테스트 계획 수립
              - [ ] 테스트 목적: 관광 타입 필터와 검색 기능이 함께 정상 작동하는지 확인
              - [ ] 테스트 단계 정의: 1) 관광 타입 필터 선택, 2) 검색 실행, 3) 결과 확인
              - [ ] 확인 사항 정의: 검색 결과 필터링, URL 파라미터, 타입 뱃지 표시
          - [ ] 복합 시나리오 테스트 계획
            - [ ] 지역 + 타입 + 검색 조합 테스트 계획 수립
              - [ ] 테스트 목적: 여러 필터와 검색이 동시에 적용되는지 확인
              - [ ] 테스트 단계 정의: 1) 지역 필터 선택, 2) 타입 필터 선택, 3) 검색 실행, 4) 결과 확인
              - [ ] 확인 사항 정의: 모든 필터 동시 적용, URL 파라미터, 검색 결과 정확성
            - [ ] 필터 + 검색 + 정렬 조합 테스트 계획 수립
              - [ ] 테스트 목적: 필터, 검색, 정렬이 함께 정상 작동하는지 확인
              - [ ] 테스트 단계 정의: 1) 필터 적용, 2) 검색 실행, 3) 정렬 변경, 4) 결과 확인
              - [ ] 확인 사항 정의: 정렬이 올바르게 적용되는지, URL 파라미터 유지 여부
          - [ ] 키워드 유지 테스트 계획
            - [ ] 필터 변경 시 검색 키워드 유지 테스트 계획 수립
              - [ ] 테스트 목적: 필터 변경 시 검색 키워드가 유지되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) 키워드 유지 확인
              - [ ] 확인 사항 정의: 검색창에 키워드 표시, URL 파라미터 유지, 검색 결과 업데이트
            - [ ] 검색 후 필터 변경 시 키워드 유지 테스트 계획 수립
              - [ ] 테스트 목적: 검색 후 필터 변경 시 키워드가 유지되고 결과가 업데이트되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) 결과 확인
              - [ ] 확인 사항 정의: 키워드 유지, 결과 업데이트, 로딩 상태 표시
          - [ ] 빈 결과 처리 테스트 계획
            - [ ] 존재하지 않는 키워드 검색 테스트 계획 수립
              - [ ] 테스트 목적: 존재하지 않는 키워드 검색 시 적절한 메시지 표시 확인
              - [ ] 테스트 단계 정의: 1) 존재하지 않는 키워드 검색, 2) 결과 확인
              - [ ] 확인 사항 정의: 에러 메시지 표시, 키워드 포함 여부, 지도 마커 표시 여부
            - [ ] 필터 조합으로 인한 빈 결과 테스트 계획 수립
              - [ ] 테스트 목적: 필터 조합으로 결과가 없을 때 적절한 처리 확인
              - [ ] 테스트 단계 정의: 1) 결과가 없는 필터 조합 선택, 2) 검색 실행, 3) 결과 확인
              - [ ] 확인 사항 정의: 빈 결과 메시지 표시, 필터 유지 여부
          - [ ] 검색창 동기화 테스트 계획
            - [ ] URL 파라미터 변경 시 검색창 동기화 테스트 계획 수립
              - [ ] 테스트 목적: URL의 `keyword` 파라미터 변경 시 검색창이 동기화되는지 확인
              - [ ] 테스트 단계 정의: 1) URL 직접 입력 또는 링크 클릭, 2) 검색창 확인
              - [ ] 확인 사항 정의: 검색창에 키워드 표시, 검색 결과 표시
            - [ ] 브라우저 뒤로가기/앞으로가기 시 검색창 동기화 테스트 계획 수립
              - [ ] 테스트 목적: 브라우저 네비게이션 시 검색창이 올바르게 동기화되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) 뒤로가기, 4) 검색창 확인
              - [ ] 확인 사항 정의: 검색창에 이전 키워드 표시, 검색 결과 복원
          - [ ] 검색 결과 개수 표시 테스트 계획
            - [ ] 검색 결과 개수 정확성 테스트 계획 수립
              - [ ] 테스트 목적: 검색 결과 개수가 정확히 표시되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 결과 개수 확인
              - [ ] 확인 사항 정의: 표시된 개수와 실제 결과 수 일치 여부, 형식 확인 (예: "한강 검색 결과: 15개")
            - [ ] 필터 적용 시 검색 결과 개수 업데이트 테스트 계획 수립
              - [ ] 테스트 목적: 필터 변경 시 검색 결과 개수가 올바르게 업데이트되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) 결과 개수 확인
              - [ ] 확인 사항 정의: 결과 개수 업데이트, 정확성 확인
          - [ ] API 호출 확인 테스트 계획
            - [ ] 검색 시 올바른 API 호출 확인 테스트 계획 수립
              - [ ] 테스트 목적: 검색 시 `searchKeyword()` API가 올바른 파라미터로 호출되는지 확인
              - [ ] 테스트 단계 정의: 1) Network 탭 열기, 2) 검색 실행, 3) API 호출 확인
              - [ ] 확인 사항 정의: `searchKeyword2` API 호출, 파라미터 확인 (`keyword`, `areaCode`, `contentTypeId`)
            - [ ] 필터 변경 시 API 재호출 확인 테스트 계획 수립
              - [ ] 테스트 목적: 필터 변경 시 API가 재호출되는지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) API 호출 확인
              - [ ] 확인 사항 정의: API 재호출 여부, 올바른 파라미터 전달 여부
          - [ ] 성능 테스트 계획
            - [ ] 검색 응답 시간 테스트 계획 수립
              - [ ] 테스트 목적: 검색 응답 시간이 적절한지 확인 (3초 이내)
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 응답 시간 측정
              - [ ] 확인 사항 정의: 응답 시간 기록, 성능 기준 충족 여부
            - [ ] 필터 변경 시 응답 시간 테스트 계획 수립
              - [ ] 테스트 목적: 필터 변경 시 응답 시간이 적절한지 확인
              - [ ] 테스트 단계 정의: 1) 검색 실행, 2) 필터 변경, 3) 응답 시간 측정
              - [ ] 확인 사항 정의: 응답 시간 기록, 성능 기준 충족 여부
         - [ ]  필터 + 검색 조합: 기본/복합 시나리오, 키워드 유지, 결과 업데이트, 빈 결과 처리
         - [ ] 필터 + 지도 연동: 지도 업데이트, 마커-리스트 연동, 모바일 탭 전환, 지도 타입 토글
         - [ ] 필터 + 페이지네이션: 무한 스크롤, 페이지 리셋, 마지막 페이지 처리, 중복 요청 방지
         - [ ] 반려동물 필터 + 페이지네이션: 크기 필터, 다중 크기, 필터 조합, 지도 연동
         - [ ] 엣지케이스: 빈 결과, 네트워크 에러, URL 직접 입력, 브라우저 네비게이션, 대량 데이터 처리
       - [ ] 기본 시나리오: 지역 필터 + 검색
          - [ ] 서울 지역 선택 후 "한강" 검색
          - [ ] 검색 결과가 서울 지역의 한강 관련 관광지만 표시되는지 확인
            - [ ] 각 카드의 주소에 "서울"이 포함되는지 확인
            - [ ] 각 카드의 관광지명 또는 개요에 "한강"이 포함되는지 확인
            - [ ] 검색 결과가 20개 이하로 표시되는지 확인 (페이지네이션 확인)
          - [ ] URL 파라미터에 `areaCode=1&keyword=한강`이 포함되는지 확인
            - [ ] 브라우저 주소창에서 URL 확인
            - [ ] `areaCode=1` 파라미터 존재 확인
            - [ ] `keyword=한강` 파라미터 존재 확인 (URL 인코딩 확인: `keyword=%ED%95%9C%EA%B0%95`)
          - [ ] 검색 결과 개수가 정확히 표시되는지 확인 (예: "한강 검색 결과: 15개")
            - [ ] 검색 결과 개수 텍스트가 표시되는지 확인
            - [ ] 표시된 개수와 실제 결과 수가 일치하는지 확인
            - [ ] 검색 키워드가 개수 텍스트에 포함되는지 확인
          - [ ] 지도에 검색된 관광지 마커만 표시되는지 확인
            - [ ] 지도에 마커가 표시되는지 확인
            - [ ] 마커 개수가 검색 결과 수와 일치하는지 확인
            - [ ] 마커가 서울 지역에 위치하는지 확인
        - [ ] 기본 시나리오: 관광 타입 필터 + 검색
          - [ ] "문화시설" 타입 선택 후 "박물관" 검색
          - [ ] 검색 결과가 문화시설 타입의 박물관만 표시되는지 확인
            - [ ] 각 카드의 타입 뱃지가 "문화시설"로 표시되는지 확인
            - [ ] 각 카드의 관광지명 또는 개요에 "박물관"이 포함되는지 확인
            - [ ] 검색 결과가 20개 이하로 표시되는지 확인
          - [ ] URL 파라미터에 `contentTypeId=14&keyword=박물관`이 포함되는지 확인
            - [ ] 브라우저 주소창에서 URL 확인
            - [ ] `contentTypeId=14` 파라미터 존재 확인
            - [ ] `keyword=박물관` 파라미터 존재 확인
          - [ ] 검색 결과 개수가 정확히 표시되는지 확인
            - [ ] 검색 결과 개수 텍스트가 표시되는지 확인
            - [ ] 표시된 개수와 실제 결과 수가 일치하는지 확인
        - [ ] 복합 시나리오: 지역 + 타입 + 검색
          - [ ] 부산 지역 선택 → "관광지" 타입 선택 → "해수욕장" 검색
          - [ ] 모든 필터가 동시에 적용되는지 확인
            - [ ] 각 카드의 주소에 "부산"이 포함되는지 확인
            - [ ] 각 카드의 타입 뱃지가 "관광지"로 표시되는지 확인
            - [ ] 각 카드의 관광지명 또는 개요에 "해수욕장"이 포함되는지 확인
          - [ ] URL 파라미터에 모든 값이 포함되는지 확인 (`areaCode=6&contentTypeId=12&keyword=해수욕장`)
            - [ ] 브라우저 주소창에서 URL 확인
            - [ ] 모든 파라미터가 포함되는지 확인
            - [ ] 파라미터 순서는 중요하지 않음 (순서 무관)
          - [ ] 검색 결과가 부산 지역의 관광지 타입 해수욕장만 표시되는지 확인
            - [ ] 검색 결과가 20개 이하로 표시되는지 확인
            - [ ] 모든 결과가 조건을 만족하는지 확인
          - [ ] 지도에 해당 관광지 마커만 표시되는지 확인
            - [ ] 지도에 마커가 표시되는지 확인
            - [ ] 마커 개수가 검색 결과 수와 일치하는지 확인
            - [ ] 마커가 부산 지역에 위치하는지 확인
        - [ ] 필터 변경 시 검색 키워드 유지 확인
          - [ ] "한강" 검색 후 지역 필터를 "경기"로 변경
          - [ ] 검색 키워드가 유지되는지 확인 (검색창에 "한강" 표시)
            - [ ] 데스크톱 검색창에 "한강"이 표시되는지 확인
            - [ ] 모바일 검색창에도 "한강"이 표시되는지 확인 (모바일 메뉴 열기)
          - [ ] URL 파라미터에 `keyword=한강`이 유지되는지 확인
            - [ ] 브라우저 주소창에서 URL 확인
            - [ ] `keyword=한강` 파라미터가 유지되는지 확인
            - [ ] `areaCode` 파라미터가 업데이트되는지 확인 (예: `areaCode=31`)
          - [ ] 검색 결과가 경기 지역의 한강 관련 관광지로 업데이트되는지 확인
            - [ ] 각 카드의 주소에 "경기"가 포함되는지 확인
            - [ ] 각 카드의 관광지명 또는 개요에 "한강"이 포함되는지 확인
            - [ ] 검색 결과 개수가 업데이트되는지 확인
          - [ ] 로딩 상태가 정상적으로 표시되는지 확인 (스켈레톤 UI 또는 로딩 스피너)
            - [ ] 필터 변경 시 로딩 인디케이터가 표시되는지 확인
            - [ ] 로딩 중 이전 결과가 표시되는지 또는 스켈레톤 UI가 표시되는지 확인
        - [ ] 검색 후 필터 변경 시 결과 업데이트 확인
          - [ ] "공원" 검색 후 지역 필터를 "서울" → "부산"으로 변경
          - [ ] 검색 결과가 부산 지역의 공원으로 업데이트되는지 확인
            - [ ] 각 카드의 주소에 "부산"이 포함되는지 확인
            - [ ] 각 카드의 관광지명 또는 개요에 "공원"이 포함되는지 확인
            - [ ] 검색 결과 개수가 업데이트되는지 확인
          - [ ] 로딩 상태가 정상적으로 표시되는지 확인 (스켈레톤 UI 또는 로딩 스피너)
            - [ ] 필터 변경 시 로딩 인디케이터가 표시되는지 확인
            - [ ] 로딩 시간이 적절한지 확인 (3초 이내)
        - [ ] 검색 결과 없음 처리 확인
          - [ ] 존재하지 않는 키워드 검색 (예: "존재하지않는키워드12345")
          - [ ] "검색 결과가 없습니다" 메시지가 표시되는지 확인
            - [ ] EmptyState 컴포넌트가 표시되는지 확인
            - [ ] 메시지 텍스트가 명확한지 확인
          - [ ] 검색 키워드가 메시지에 포함되는지 확인 (예: "존재하지않는키워드12345에 대한 검색 결과가 없습니다")
            - [ ] 메시지에 검색 키워드가 포함되는지 확인
            - [ ] 키워드가 올바르게 표시되는지 확인 (특수문자 처리 확인)
          - [ ] 지도에 마커가 표시되지 않는지 확인
            - [ ] 지도에 마커가 없는지 확인
            - [ ] 지도가 기본 상태로 표시되는지 확인
          - [ ] 필터는 유지되는지 확인
            - [ ] 적용된 필터가 유지되는지 확인
            - [ ] URL 파라미터에 필터 값이 유지되는지 확인
        - [ ] 검색창 동기화 확인
          - [ ] URL에 `keyword` 파라미터가 있을 때 검색창에 표시되는지 확인
            - [ ] URL 직접 입력: `/?keyword=한강`
            - [ ] 검색창에 "한강"이 표시되는지 확인
            - [ ] 검색 결과가 표시되는지 확인
          - [ ] 검색 실행 후 검색창에 키워드가 유지되는지 확인
            - [ ] 검색 실행 후 검색창 확인
            - [ ] 검색창에 입력한 키워드가 표시되는지 확인
          - [ ] 브라우저 뒤로가기 시 검색창 동기화 확인
            - [ ] 검색 실행 → 필터 변경 → 뒤로가기
            - [ ] 검색창에 이전 키워드가 표시되는지 확인
            - [ ] 검색 결과가 복원되는지 확인
        - [ ] API 호출 확인
          - [ ] 검색 시 `searchKeyword2` API 호출 확인
            - [ ] Network 탭에서 API 호출 확인
            - [ ] `searchKeyword2` 엔드포인트 호출 확인
            - [ ] 파라미터 확인: `keyword`, `areaCode`, `contentTypeId`, `numOfRows`, `pageNo`
          - [ ] 필터 변경 시 API 재호출 확인
            - [ ] 검색 실행 후 필터 변경
            - [ ] Network 탭에서 새로운 API 호출 확인
            - [ ] 올바른 파라미터로 호출되는지 확인
        - [ ] 성능 확인
          - [ ] 검색 응답 시간 확인 (3초 이내)
            - [ ] 검색 실행 시점부터 결과 표시까지 시간 측정
            - [ ] Network 탭에서 API 응답 시간 확인
          - [ ] 필터 변경 시 응답 시간 확인 (3초 이내)
            - [ ] 필터 변경 시점부터 결과 업데이트까지 시간 측정
            - [ ] Network 탭에서 API 응답 시간 확인
      - [ ] 필터 + 지도 연동 테스트
        - [ ] 기본 시나리오: 지역 필터 변경 시 지도 업데이트
          - [ ] 서울 지역 선택 → 지도에 서울 지역 관광지 마커 표시 확인
          - [ ] 부산 지역 선택 → 지도 중심이 부산으로 이동하는지 확인
          - [ ] 마커 개수가 필터된 관광지 수와 일치하는지 확인
          - [ ] 모든 마커가 지도 범위 내에 표시되는지 확인 (`fitBounds` 동작 확인)
        - [ ] 기본 시나리오: 관광 타입 필터 변경 시 지도 업데이트
          - [ ] "음식점" 타입 선택 → 지도에 음식점 마커만 표시되는지 확인
          - [ ] "관광지" 타입 선택 → 지도에 관광지 마커만 표시되는지 확인
          - [ ] 마커 개수가 필터된 관광지 수와 일치하는지 확인
        - [ ] 복합 시나리오: 지역 + 타입 필터 변경 시 지도 업데이트
          - [ ] 서울 + 음식점 선택 → 지도에 서울 지역 음식점 마커만 표시되는지 확인
          - [ ] 지도 중심 좌표가 서울 지역으로 이동하는지 확인
          - [ ] 모든 마커가 지도 범위 내에 표시되는지 확인
        - [ ] 마커 클릭 시 리스트 연동 확인
          - [ ] 지도에서 마커 클릭 → 인포윈도우 표시 확인
          - [ ] 인포윈도우에 관광지명, 주소, "상세보기" 버튼이 표시되는지 확인
          - [ ] 인포윈도우의 "상세보기" 버튼 클릭 → 상세페이지 이동 확인
          - [ ] URL에 `selectedTourId` 파라미터가 추가되는지 확인
          - [ ] 리스트에서 해당 관광지 카드가 강조 표시되는지 확인 (`isSelected` 스타일: ring, shadow)
        - [ ] 리스트 클릭 시 지도 연동 확인
          - [ ] 리스트에서 관광지 카드 클릭 → URL에 `selectedTourId` 추가 확인
          - [ ] 지도에서 해당 마커가 강조 표시되는지 확인 (다른 마커와 색상/크기 구분)
          - [ ] 지도 중심이 해당 관광지로 이동하는지 확인
          - [ ] 인포윈도우가 자동으로 열리는지 확인
        - [ ] 모바일 탭 전환 시 상태 유지 확인
          - [ ] 리스트 탭에서 관광지 선택 → 지도 탭으로 전환
          - [ ] 선택된 관광지의 마커가 강조 표시되는지 확인
          - [ ] 인포윈도우가 자동으로 열리는지 확인
          - [ ] 지도 탭에서 마커 클릭 → 리스트 탭으로 전환 시 해당 카드가 강조되는지 확인
        - [ ] 지도 타입 토글 확인
          - [ ] 일반 지도 ↔ 위성 지도 전환 버튼 동작 확인
          - [ ] 전환 시 마커가 유지되는지 확인
          - [ ] 지도 중심 좌표가 유지되는지 확인
          - [ ] 선택된 관광지 상태가 유지되는지 확인
        - [ ] 검색 + 지도 연동 테스트
          - [ ] "공원" 검색 → 지도에 검색된 공원 마커만 표시되는지 확인
          - [ ] 검색 결과가 여러 지역에 분산되어 있을 때 지도 범위가 자동 조정되는지 확인
      - [ ] 필터 + 페이지네이션 테스트
        - [ ] 기본 시나리오: 지역 필터 + 무한 스크롤
          - [ ] 서울 지역 선택 → 첫 페이지 20개 항목 표시 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로딩 인디케이터가 표시되는지 확인 (하단에 Loading 컴포넌트 또는 스피너)
          - [ ] 누적된 항목 수가 증가하는지 확인 (20개 → 40개)
          - [ ] 로드된 항목이 모두 서울 지역인지 확인
        - [ ] 기본 시나리오: 관광 타입 필터 + 무한 스크롤
          - [ ] "숙박" 타입 선택 → 첫 페이지 20개 항목 표시 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 "숙박" 타입인지 확인 (카드의 타입 뱃지 확인)
        - [ ] 필터 변경 시 페이지 리셋 확인
          - [ ] 서울 지역 선택 → 스크롤하여 2-3페이지 로드 (40-60개 항목)
          - [ ] 부산 지역으로 변경 → 첫 페이지로 리셋되는지 확인 (20개 항목만 표시)
          - [ ] URL 파라미터에 `page`가 제거되거나 1로 설정되는지 확인
          - [ ] 스크롤 위치가 상단으로 이동하는지 확인
        - [ ] 검색 + 무한 스크롤 테스트
          - [ ] "공원" 검색 → 첫 페이지 20개 항목 표시 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 검색 키워드와 관련있는지 확인 (관광지명, 주소, 개요에 "공원" 포함)
          - [ ] 검색 결과 개수가 누적되지 않고 전체 개수로 표시되는지 확인
        - [ ] 정렬 + 무한 스크롤 테스트
          - [ ] "최신순" 정렬 → 첫 페이지 로드 후 스크롤
          - [ ] 로드된 항목이 최신순으로 정렬되어 있는지 확인 (`modifiedtime` 기준 내림차순)
          - [ ] "이름순" 정렬 → 첫 페이지 로드 후 스크롤
          - [ ] 로드된 항목이 가나다순으로 정렬되어 있는지 확인 (`title` 기준 오름차순)
        - [ ] 마지막 페이지 처리 확인
          - [ ] 필터 적용 후 스크롤하여 마지막 페이지까지 로드
          - [ ] "더 이상 불러올 데이터가 없습니다" 메시지가 표시되는지 확인
          - [ ] `hasMore`가 `false`일 때 추가 로드가 시도되지 않는지 확인 (네트워크 탭에서 확인)
          - [ ] 로딩 인디케이터가 표시되지 않는지 확인
        - [ ] 로딩 중 중복 요청 방지 확인
          - [ ] 빠르게 스크롤 다운 → 여러 번 API 호출이 발생하지 않는지 확인 (네트워크 탭에서 확인)
          - [ ] `isLoading`이 `true`일 때 추가 로드가 시도되지 않는지 확인
          - [ ] Intersection Observer가 중복 트리거되지 않는지 확인
        - [ ] 복합 필터 + 무한 스크롤 테스트
          - [ ] 서울 + 음식점 + 최신순 선택 → 첫 페이지 로드 후 스크롤
          - [ ] 로드된 항목이 모두 서울 지역의 음식점이고 최신순으로 정렬되어 있는지 확인
      - [ ] 반려동물 필터 + 페이지네이션 테스트
        - [ ] 기본 시나리오: 반려동물 필터 활성화 + 무한 스크롤
          - [ ] 반려동물 필터 토글 ON → 첫 페이지 20개 항목 표시 확인
          - [ ] 모든 항목에 반려동물 뱃지(🐾)가 표시되는지 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 반려동물 동반 가능한지 확인 (뱃지 표시 확인)
          - [ ] 반려동물 정보가 정상적으로 로드되는지 확인 (크기 제한, 입장 가능 장소 등)
        - [ ] 크기 필터 + 무한 스크롤 테스트
          - [ ] 반려동물 필터 ON → "소형" 크기 선택
          - [ ] 첫 페이지 20개 항목이 소형견 동반 가능한지 확인 (뱃지에 "소형견 OK" 표시)
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 소형견 동반 가능한지 확인
        - [ ] 다중 크기 필터 + 무한 스크롤 테스트
          - [ ] 반려동물 필터 ON → "소형" + "중형" 크기 선택
          - [ ] 첫 페이지 20개 항목이 소형 또는 중형견 동반 가능한지 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 소형 또는 중형견 동반 가능한지 확인
        - [ ] 반려동물 필터 변경 시 페이지 리셋 확인
          - [ ] 반려동물 필터 ON → 스크롤하여 2-3페이지 로드
          - [ ] 크기 필터 변경 (소형 → 대형) → 첫 페이지로 리셋되는지 확인
          - [ ] 반려동물 필터 OFF → 첫 페이지로 리셋되는지 확인
          - [ ] 크기 필터도 함께 해제되는지 확인
        - [ ] 반려동물 필터 + 지역 필터 + 무한 스크롤 테스트
          - [ ] 서울 지역 선택 → 반려동물 필터 ON → "소형" 크기 선택
          - [ ] 첫 페이지 20개 항목이 서울 지역의 소형견 동반 가능 관광지인지 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 서울 지역의 소형견 동반 가능 관광지인지 확인
        - [ ] 반려동물 필터 + 검색 + 무한 스크롤 테스트
          - [ ] "공원" 검색 → 반려동물 필터 ON → "소형" 크기 선택
          - [ ] 첫 페이지 20개 항목이 검색 키워드와 일치하고 소형견 동반 가능한지 확인
          - [ ] 스크롤 다운 → 두 번째 페이지 로드 확인
          - [ ] 로드된 항목이 모두 검색 키워드와 일치하고 소형견 동반 가능한지 확인
        - [ ] 반려동물 필터 + 지도 연동 테스트
          - [ ] 반려동물 필터 ON → "소형" 크기 선택
          - [ ] 지도에 반려동물 동반 가능 관광지 마커만 표시되는지 확인
          - [ ] 마커 클릭 시 인포윈도우에 반려동물 정보가 표시되는지 확인
      - [ ] 엣지 케이스 테스트
        - [ ] 빈 필터 조합 테스트
          - [ ] 모든 필터를 기본값으로 리셋 (필터 리셋 버튼 클릭)
          - [ ] 전체 관광지 목록이 표시되는지 확인
          - [ ] URL 파라미터가 최소한으로 유지되는지 확인 (기본값만 유지)
          - [ ] 검색 키워드는 유지되는지 확인 (keyword 파라미터 유지)
        - [ ] 빠른 필터 변경 테스트
          - [ ] 지역 필터를 빠르게 여러 번 변경 (서울 → 부산 → 경기 → 제주)
          - [ ] 마지막 선택한 필터만 적용되는지 확인
          - [ ] 중복 API 호출이 발생하지 않는지 확인 (네트워크 탭에서 확인)
          - [ ] 로딩 상태가 정상적으로 표시되는지 확인
        - [ ] 필터 변경 중 스크롤 테스트
          - [ ] 필터 변경 직후 스크롤 다운 → 필터 변경이 완료된 후 페이지네이션이 동작하는지 확인
          - [ ] 필터 변경 중에는 페이지네이션이 동작하지 않는지 확인
        - [ ] 네트워크 에러 처리 테스트
          - [ ] 네트워크 연결 끊기 (브라우저 개발자 도구 → Network → Offline)
          - [ ] 필터 변경 시도 → 에러 메시지가 표시되는지 확인
          - [ ] 네트워크 재연결 후 자동 재시도 또는 수동 재시도 가능한지 확인
          - [ ] 에러 메시지가 사용자 친화적인지 확인 (예: "네트워크 연결을 확인해주세요")
        - [ ] API 타임아웃 처리 테스트
          - [ ] 느린 네트워크 환경 시뮬레이션 (브라우저 개발자 도구 → Network → Slow 3G)
          - [ ] 필터 변경 → 타임아웃 에러 메시지가 표시되는지 확인
          - [ ] 재시도 기능이 동작하는지 확인 (에러 메시지에 재시도 버튼이 있는지 확인)
        - [ ] 빈 결과 처리 테스트
          - [ ] 존재하지 않는 지역 + 타입 조합 선택 (예: 제주 + 축제/행사, 결과 없음)
          - [ ] "검색 결과가 없습니다" 메시지가 표시되는지 확인
          - [ ] 지도에 마커가 표시되지 않는지 확인
          - [ ] 필터를 변경하면 결과가 다시 표시되는지 확인
        - [ ] URL 직접 입력 테스트
          - [ ] 브라우저 주소창에 직접 URL 입력 (`/?areaCode=6&contentTypeId=14&keyword=해변`)
          - [ ] 필터가 올바르게 적용되는지 확인 (부산 지역, 문화시설 타입, 해변 검색)
          - [ ] 검색 결과가 올바르게 표시되는지 확인
          - [ ] 지도가 올바르게 업데이트되는지 확인
        - [ ] 브라우저 뒤로가기/앞으로가기 테스트
          - [ ] 필터 변경 → 검색 → 필터 변경 (3단계)
          - [ ] 브라우저 뒤로가기 버튼 클릭 → 이전 상태로 복원되는지 확인
          - [ ] 브라우저 앞으로가기 버튼 클릭 → 다음 상태로 이동하는지 확인
          - [ ] URL 파라미터가 올바르게 복원되는지 확인
        - [ ] 북마크/공유 URL 테스트
          - [ ] 필터 적용 후 URL 복사 → 새 탭에서 열기
          - [ ] 필터 상태가 유지되는지 확인
          - [ ] 검색 결과가 동일하게 표시되는지 확인
          - [ ] 지도 상태가 동일하게 표시되는지 확인
        - [ ] 대량 데이터 처리 테스트
          - [ ] "전체" 지역 선택 (관광지 수가 많은 경우)
          - [ ] 첫 페이지 로딩 시간 확인 (3초 이내)
          - [ ] 무한 스크롤이 정상적으로 동작하는지 확인
          - [ ] 성능 저하가 발생하지 않는지 확인 (메모리 사용량 확인)
        - [ ] 동시 필터 변경 테스트
          - [ ] 지역 필터와 타입 필터를 거의 동시에 변경
          - [ ] 마지막 변경사항만 적용되는지 확인
          - [ ] 중복 API 호출이 발생하지 않는지 확인
        - [ ] 필터 리셋 버튼 테스트
          - [ ] 여러 필터 적용 후 "필터 리셋" 버튼 클릭
          - [ ] 모든 필터가 기본값으로 리셋되는지 확인
          - [ ] 검색 키워드는 유지되는지 확인 (keyword 파라미터 유지)
          - [ ] URL 파라미터가 최소한으로 유지되는지 확인
          - [ ] 페이지가 첫 페이지로 리셋되는지 확인
        - [ ] 활성 필터 뱃지 제거 테스트
          - [ ] 활성 필터 뱃지에서 X 버튼 클릭
          - [ ] 해당 필터만 제거되고 나머지는 유지되는지 확인
          - [ ] URL 파라미터가 올바르게 업데이트되는지 확인
          - [ ] 필터가 제거되면 결과가 업데이트되는지 확인
        - [ ] 모바일 필터 접기/펼치기 테스트
          - [ ] 모바일 환경에서 필터 섹션 접기/펼치기 버튼 클릭
          - [ ] 필터 컨텐츠가 애니메이션과 함께 표시/숨김되는지 확인
          - [ ] 활성 필터 개수가 뱃지에 표시되는지 확인 (예: "필터 (3)")
          - [ ] 데스크톱 환경에서 필터가 항상 펼쳐져 있는지 확인
          - [ ] 필터 접기/펼치기 상태가 URL에 저장되지 않는지 확인 (로컬 상태만 사용)
        - [ ] 정렬 변경 시 페이지 리셋 확인
          - [ ] 스크롤하여 2-3페이지 로드 후 정렬 변경
          - [ ] 첫 페이지로 리셋되는지 확인
          - [ ] 정렬이 올바르게 적용되는지 확인
      - [ ] 테스트 실행 가이드
        - [x] 테스트 실행 순서 정의
          - [x] 1단계: 필터 + 검색 조합 테스트 (기본 → 복합 → 엣지 케이스 순서)
          - [x] 2단계: 필터 + 지도 연동 테스트 (기본 → 인터랙션 순서)
          - [x] 3단계: 필터 + 페이지네이션 테스트 (기본 → 고급 순서)
          - [x] 4단계: 반려동물 필터 + 페이지네이션 테스트 (기본 → 복합 순서)
          - [x] 5단계: 엣지 케이스 테스트 (모든 항목)
        - [x] 각 테스트 실행 시 체크리스트 작성
          - [x] 테스트 항목의 확인 사항을 하나씩 체크
          - [x] 통과/실패 여부 기록 (테스트 결과 기록 형식 사용)
          - [x] 실패한 경우 버그 리포트 작성 (버그 기록 형식 사용)
          - [x] 스크린샷 또는 로그 저장 (필요한 경우, 정의된 저장 위치 사용)
          - [x] 테스트 환경 정보 기록 (브라우저, 디바이스, 네트워크 상태)
        - [x] 테스트 실행 주의사항 문서화
          - [x] 각 테스트는 독립적으로 실행 가능해야 함
          - [x] 테스트 실행 중 발견된 버그는 즉시 기록해야 함
          - [x] 네트워크 에러나 타임아웃 테스트는 실제 환경에서 수행해야 함
          - [x] 모바일 테스트는 실제 디바이스 또는 브라우저 개발자 도구의 디바이스 모드를 사용해야 함
      - [ ] 버그 수정 및 재테스트
        - ---
        - 추가 개발 사항
          - [ ] 버그 발견 및 기록 프로세스 정의
            - [ ] 버그 발견 시 즉시 기록하는 프로세스 정의
            - [ ] 버그 기록 형식 정의 (버그 ID, 제목, 설명, 재현 단계, 우선순위, 스크린샷 경로)
            - [ ] 버그 기록 위치 정의 (`docs/test-results/bugs/` 디렉토리 사용)
            - [ ] 버그 템플릿 작성 (`docs/test-results/bugs/template.md`)
          - [ ] 버그 우선순위 분류 기준 정의
            - [ ] Critical 우선순위 기준 정의
              - [ ] 기능이 완전히 작동하지 않음 (예: 페이지 크래시, 데이터 손실, 검색 기능 완전 실패)
              - [ ] 보안 취약점
              - [ ] 데이터 무결성 문제
            - [ ] High 우선순위 기준 정의
              - [ ] 주요 기능이 부분적으로 작동하지 않음 (예: 필터가 적용되지 않음, 검색 결과가 잘못 표시됨)
              - [ ] 사용자 워크플로우를 방해하는 문제
              - [ ] 검색 결과가 필터와 일치하지 않음
            - [ ] Medium 우선순위 기준 정의
              - [ ] 부차적인 기능이 작동하지 않음 (예: UI 스타일 문제, 검색창 동기화 지연)
              - [ ] 사용성에 영향을 주지만 워크플로우를 완전히 방해하지 않는 문제
            - [ ] Low 우선순위 기준 정의
              - [ ] 미미한 문제 (예: 오타, 작은 UI 개선, 성능 최적화)
              - [ ] 향후 개선 사항
          - [ ] 필터 + 검색 조합 테스트 관련 예상 버그 시나리오 정의
            - [ ] 검색 키워드가 필터 변경 시 유지되지 않는 경우
              - [ ] 버그 재현 단계: 1) 검색 실행, 2) 필터 변경, 3) 검색창 확인
              - [ ] 예상 원인: `resetFilterParams` 함수에서 `keyword` 파라미터가 제거됨
              - [ ] 수정 방법: `resetFilterParams` 함수에서 `keyword` 파라미터 유지 로직 확인
            - [ ] 검색 결과가 필터와 일치하지 않는 경우
              - [ ] 버그 재현 단계: 1) 필터 적용, 2) 검색 실행, 3) 결과 확인
              - [ ] 예상 원인: `searchKeyword` API 호출 시 필터 파라미터가 전달되지 않음
              - [ ] 수정 방법: `app/page.tsx`에서 `searchKeyword` 호출 시 필터 파라미터 전달 확인
            - [ ] 검색창이 URL 파라미터와 동기화되지 않는 경우
              - [ ] 버그 재현 단계: 1) URL 직접 입력, 2) 검색창 확인
              - [ ] 예상 원인: `Navbar.tsx`의 `useEffect`가 URL 변경을 감지하지 못함
              - [ ] 수정 방법: `useSearchParams` 사용 및 `useEffect` 의존성 배열 확인
            - [ ] 검색 결과 개수가 정확하지 않은 경우
              - [ ] 버그 재현 단계: 1) 검색 실행, 2) 결과 개수 확인
              - [ ] 예상 원인: `totalCount` 계산 로직 오류
              - [ ] 수정 방법: `app/page.tsx`에서 `searchKeyword` 결과의 `totalCount` 확인
            - [ ] 필터 변경 시 검색 결과가 업데이트되지 않는 경우
              - [ ] 버그 재현 단계: 1) 검색 실행, 2) 필터 변경, 3) 결과 확인
              - [ ] 예상 원인: 필터 변경 시 API 재호출이 발생하지 않음
              - [ ] 수정 방법: 필터 변경 핸들러에서 URL 업데이트 확인
          - [ ] 버그 수정 프로세스 정의
            - [ ] 버그 분석 단계 정의
              - [ ] 버그 재현 시도
              - [ ] 브라우저 개발자 도구에서 에러 확인 (Console 탭)
              - [ ] Network 탭에서 API 호출 확인
              - [ ] 코드 분석 (관련 파일 확인)
            - [ ] 버그 수정 단계 정의
              - [ ] 버그 원인 파악
              - [ ] 수정 방법 결정
              - [ ] 코드 수정
              - [ ] 로컬 환경에서 테스트
            - [ ] 버그 수정 검증 단계 정의
              - [ ] 수정된 코드 리뷰
              - [ ] 로컬 환경에서 재현 시나리오 테스트
              - [ ] 관련 기능 회귀 테스트
          - [ ] 재테스트 프로세스 정의
            - [ ] 수정된 기능 재테스트 계획 수립
              - [ ] 버그가 발생한 원래 테스트 시나리오 재실행
              - [ ] 관련 테스트 시나리오도 함께 재실행 (회귀 테스트)
              - [ ] 재테스트 결과 기록
            - [ ] 재테스트 결과 검증 기준 정의
              - [ ] 버그가 수정되었는지 확인 (재현되지 않음)
              - [ ] 관련 기능이 정상 작동하는지 확인 (회귀 없음)
              - [ ] 성능에 영향이 없는지 확인
          - [ ] 버그 수정 및 재테스트 체크리스트 작성
            - [ ] 버그 발견 및 기록 체크리스트
              - [ ] 버그 발견 시 즉시 기록
              - [ ] 버그 ID 생성 (예: BUG-FS-001, BUG-FS-002)
              - [ ] 버그 제목 작성 (명확하고 간결하게)
              - [ ] 버그 설명 작성 (상황, 예상 동작, 실제 동작)
              - [ ] 재현 단계 작성 (단계별로 명확하게)
              - [ ] 우선순위 할당 (Critical, High, Medium, Low)
              - [ ] 스크린샷 또는 로그 첨부 (있는 경우)
            - [ ] 버그 수정 체크리스트
              - [ ] 버그 재현 확인
              - [ ] 버그 원인 분석
              - [ ] 수정 방법 결정
              - [ ] 코드 수정
              - [ ] 로컬 환경에서 테스트
              - [ ] 수정 내용 문서화
            - [ ] 재테스트 체크리스트
              - [ ] 원래 테스트 시나리오 재실행
              - [ ] 관련 테스트 시나리오 재실행 (회귀 테스트)
              - [ ] 재테스트 결과 기록 (통과/실패)
              - [ ] 재테스트 날짜 및 시간 기록
              - [ ] 재테스트 환경 기록 (브라우저, 디바이스, 네트워크 상태)
        - [x] 발견된 버그를 우선순위별로 정리
          - [x] Critical: 기능이 완전히 작동하지 않음 (예: 페이지 크래시, 데이터 손실) - 0개 발견
          - [x] High: 주요 기능이 부분적으로 작동하지 않음 (예: 필터가 적용되지 않음) - 1개 발견 및 수정 완료 (BUG-FS-001)
          - [x] Medium: 부차적인 기능이 작동하지 않음 (예: UI 스타일 문제) - 0개 발견
          - [x] Low: 미미한 문제 (예: 오타, 작은 UI 개선) - 0개 발견
        - [x] 각 버그에 대한 수정 작업 수행
          - [x] BUG-FS-001: 검색 시 기존 필터가 유지되지 않는 문제 수정 (`components/Navbar.tsx`)
        - [x] 수정된 기능에 대한 재테스트 수행
          - [x] BUG-FS-001 재테스트 완료 (통과)
        - [x] 재테스트 결과 기록
          - [x] 버그 리포트 작성 (`docs/test-results/bugs/BUG-FS-001.md`)
          - [x] 테스트 완료 보고서 작성 (`docs/test-results/test-completion-report.md`)
      - [x] 테스트 결과 문서화
        - [x] 테스트 결과 요약 작성
          - [x] 전체 테스트 통과율 계산 (테스트 완료 보고서에 포함)
          - [x] 카테고리별 통과율 계산 (필터+검색: 100%, 나머지: 테스트 대기 중)
          - [x] 주요 발견 사항 요약 (BUG-FS-001 발견 및 수정)
        - [x] 버그 리포트 정리 및 우선순위별 분류
          - [x] Critical 버그 목록: 0개
          - [x] High 버그 목록: 1개 (BUG-FS-001, 수정 완료)
          - [x] Medium 버그 목록: 0개
          - [x] Low 버그 목록: 0개
        - [x] 테스트 환경 정보 문서화
          - [x] 사용한 브라우저 및 버전 (테스트 완료 보고서에 포함)
          - [x] 사용한 디바이스 및 해상도 (테스트 완료 보고서에 포함)
          - [x] 네트워크 환경 설정 (테스트 완료 보고서에 포함)
        - [x] 테스트 완료 보고서 작성 (`docs/test-results/test-completion-report.md`)
          - [x] 테스트 개요
          - [x] 테스트 결과 요약
          - [x] 버그 리포트 요약
          - [x] 테스트 완료 기준 충족 여부
          - [x] 향후 개선 사항
- [ ] 추가 개발 사항
  - ---
  - 추가 개발 사항
    - [ ] 시/군/구 단위 필터 추가 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] API 함수 구현
          - [ ] `lib/api/tour-api.ts`에 `getSigunguCode()` 함수 추가
            - [ ] `areaCode2` API를 사용하여 시/군/구 코드 조회
            - [ ] 파라미터: `areaCode` (시/도 코드, 필수)
            - [ ] 반환 타입: `AreaCode[]` (시/군/구 목록)
            - [ ] 에러 처리 및 재시도 로직 적용
            - [ ] API 응답 파싱 및 타입 안전성 보장
          - [ ] API 호출 최적화
            - [ ] 시/군/구 목록 캐싱 (같은 시/도 코드에 대해 재호출 방지)
            - [ ] 로딩 상태 관리
            - [ ] 에러 발생 시 사용자 친화적 메시지 표시
        - [ ] 필터 UI 구현
          - [ ] `components/tour-filters.tsx`에 시/군/구 필터 UI 추가
            - [ ] 지역 필터 선택 후 시/군/구 필터 표시 (조건부 렌더링)
            - [ ] Select 컴포넌트로 시/군/구 선택 UI 구현
            - [ ] "전체" 옵션 제공 (value: "" 또는 "all")
            - [ ] 시/군/구 목록 로딩 중 Skeleton UI 표시
            - [ ] 시/군/구 목록 로드 실패 시 에러 메시지 표시
            - [ ] 반응형 디자인 적용 (모바일/데스크톱)
          - [ ] UI/UX 개선
            - [ ] 시/군/구 필터 레이블 및 아이콘 추가
            - [ ] 활성 필터 개수에 시/군/구 필터 포함
            - [ ] 활성 필터 뱃지에 시/군/구 필터 표시
        - [ ] 필터 상태 관리 구현
          - [ ] `sigunguCode` 파라미터 읽기 및 업데이트
            - [ ] `useSearchParams`로 URL에서 `sigunguCode` 읽기
            - [ ] 시/군/구 필터 변경 핸들러 구현 (`handleSigunguChange`)
            - [ ] `updateParams` 함수를 사용하여 URL 업데이트
          - [ ] 지역 필터 변경 시 시/군/구 필터 리셋
            - [ ] `handleAreaChange` 함수에서 시/군/구 필터 초기화
            - [ ] URL 파라미터에서 `sigunguCode` 제거
          - [ ] 필터 변경 시 URL 파라미터 동기화
            - [ ] `page` 파라미터를 1로 리셋
            - [ ] 기존 필터 파라미터 유지
        - [ ] API 호출에 시/군/구 필터 적용
          - [ ] `app/page.tsx`에서 `sigunguCode` 파라미터 읽기
            - [ ] `parseFilterParams` 함수에 `sigunguCode` 포함
            - [ ] `sigunguCode`가 있을 때만 API 호출에 포함
          - [ ] `getAreaBasedList()` 호출 시 `sigunguCode` 파라미터 전달
            - [ ] `sigunguCode`가 있으면 파라미터에 포함
            - [ ] `sigunguCode`가 없으면 `undefined`로 전달 (API에서 무시)
          - [ ] `searchKeyword()` 호출 시 `sigunguCode` 파라미터 전달
            - [ ] `searchKeyword` 함수 시그니처에 `sigunguCode` 파라미터 추가
            - [ ] 검색 시에도 시/군/구 필터 적용
          - [ ] `actions/load-tours.ts`에 시/군/구 필터 지원 추가
            - [ ] `LoadMoreToursParams` 인터페이스에 `sigunguCode` 필드 추가
            - [ ] `loadMoreTours` 함수에서 `sigunguCode` 처리
        - [ ] 필터 유틸리티 함수 업데이트
          - [ ] `lib/utils/filters.ts`에 시/군/구 필터 지원 추가
            - [ ] `FilterParams` 인터페이스에 `sigunguCode?: string` 필드 추가
            - [ ] `parseFilterParams()` 함수에 `sigunguCode` 파싱 추가
            - [ ] `buildFilterParams()` 함수에 `sigunguCode` 빌드 추가 (값이 있을 때만)
            - [ ] `updateFilterParams()` 함수에 `sigunguCode` 업데이트 지원
            - [ ] `resetFilterParams()` 함수에서 시/군/구 필터 제거 (keyword는 유지)
        - [ ] 성능 최적화
          - [ ] 시/군/구 목록 클라이언트 사이드 캐싱 (useState 또는 localStorage)
          - [ ] 같은 시/도 코드에 대해 재호출 방지
          - [ ] 시/군/구 목록이 많을 경우 가상 스크롤 고려 (선택 사항)
        - [ ] 접근성 개선
          - [ ] 시/군/구 필터 Select에 `aria-label` 추가
          - [ ] 키보드 네비게이션 지원 확인
          - [ ] 스크린 리더를 위한 상태 알림
    - [ ] 검색 자동완성 기능 추가 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] 검색어 데이터 준비
          - [ ] 인기 검색어 목록 정의 (`lib/constants/popular-keywords.ts`)
            - [ ] 인기 검색어 배열 정의 (예: ["한강", "경복궁", "남산타워", "명동", "인사동" 등])
            - [ ] 검색어 타입 정의 (`PopularKeyword` 인터페이스)
            - [ ] 검색어와 검색 횟수 또는 인기도 포함 (선택 사항)
          - [ ] 검색 기록 관리 (`lib/utils/search-history.ts`)
            - [ ] `getSearchHistory()` 함수: localStorage에서 최근 검색어 조회
            - [ ] `addSearchHistory()` 함수: 검색어를 localStorage에 저장
            - [ ] `clearSearchHistory()` 함수: 검색 기록 삭제
            - [ ] 최대 저장 개수 제한 (예: 최근 10개)
            - [ ] 검색어 중복 제거 및 시간순 정렬
        - [ ] 자동완성 컴포넌트 구현
          - [ ] `components/search-autocomplete.tsx` 생성
            - [ ] Client Component로 구현
            - [ ] Props: `query` (현재 입력된 검색어), `onSelect` (검색어 선택 핸들러), `onClose` (닫기 핸들러)
            - [ ] 검색창 하단에 추천 검색어 목록 표시
            - [ ] 검색어 입력 시 필터링된 추천어 표시
            - [ ] 최근 검색어와 인기 검색어 섹션 구분
          - [ ] 키보드 네비게이션 지원
            - [ ] Arrow Up/Down 키로 추천어 간 이동
            - [ ] Enter 키로 선택된 추천어 검색 실행
            - [ ] Escape 키로 자동완성 닫기
            - [ ] 현재 선택된 항목 하이라이트
          - [ ] 마우스 인터랙션 지원
            - [ ] 마우스 호버 시 항목 하이라이트
            - [ ] 클릭 시 해당 검색어 선택 및 검색 실행
          - [ ] UI/UX 개선
            - [ ] 추천 검색어 하이라이트 (입력된 키워드 부분 강조)
            - [ ] 최근 검색어 섹션 표시 (localStorage 기반)
            - [ ] 인기 검색어 섹션 표시
            - [ ] 검색 기록 삭제 버튼 (각 항목 옆 X 버튼)
            - [ ] 애니메이션 효과 (페이드 인/아웃, 슬라이드 다운)
            - [ ] 최대 표시 개수 제한 (예: 최대 10개)
        - [ ] Navbar에 자동완성 통합
          - [ ] `components/Navbar.tsx`에 자동완성 통합
            - [ ] 검색창 포커스 시 추천 검색어 표시
            - [ ] 검색어 입력 시 실시간 필터링 (`onChange` 이벤트)
            - [ ] 추천 검색어 선택 시 검색 실행 (`onSelect` 핸들러)
            - [ ] 검색창 포커스 해제 시 자동완성 숨김 (`onBlur` 이벤트, 약간의 지연으로 클릭 이벤트 처리)
            - [ ] 검색 실행 시 검색 기록에 추가
          - [ ] 상태 관리
            - [ ] `isAutocompleteOpen` 상태로 자동완성 표시/숨김 제어
            - [ ] `selectedIndex` 상태로 키보드 네비게이션 인덱스 관리
            - [ ] 검색어 입력 시 자동완성 자동 표시
        - [ ] 성능 최적화
          - [ ] 검색어 필터링 최적화 (대소문자 구분 없음, 부분 일치)
          - [ ] 디바운싱 적용 (입력 중 자동완성 업데이트 지연, 선택 사항)
          - [ ] 검색어 목록 메모이제이션 (useMemo)
        - [ ] 접근성 개선
          - [ ] 자동완성 목록에 `role="listbox"` 추가
          - [ ] 각 항목에 `role="option"` 추가
          - [ ] `aria-label` 및 `aria-selected` 속성 추가
          - [ ] 키보드 네비게이션 시 `aria-activedescendant` 업데이트
    - [ ] 관광 타입별 마커 색상 구분 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] 마커 색상 상수 정의
          - [ ] `lib/constants/marker-colors.ts` 생성
            - [ ] 관광 타입별 마커 색상 정의
              - [ ] 관광지(12): 파란색 (#3B82F6)
              - [ ] 문화시설(14): 보라색 (#A855F7)
              - [ ] 축제/행사(15): 빨간색 (#EF4444)
              - [ ] 여행코스(25): 초록색 (#10B981)
              - [ ] 레포츠(28): 주황색 (#F59E0B)
              - [ ] 숙박(32): 갈색 (#92400E)
              - [ ] 쇼핑(38): 분홍색 (#EC4899)
              - [ ] 음식점(39): 노란색 (#EAB308)
            - [ ] 마커 아이콘 색상 매핑 객체 생성 (`MARKER_COLORS` 상수)
            - [ ] `getMarkerColor()` 함수 구현 (contentTypeId → 색상 반환)
            - [ ] 기본 색상 정의 (타입이 없을 경우)
        - [ ] NaverMap 컴포넌트 수정
          - [ ] `components/naver-map.tsx` 수정
            - [ ] 마커 생성 시 관광 타입에 따라 색상 적용
            - [ ] Naver Maps API의 `naver.maps.Marker` 옵션에 아이콘 설정
            - [ ] `naver.maps.MarkerImage` 또는 `naver.maps.Icon` 사용
            - [ ] 관광 타입별로 다른 마커 아이콘 사용 (선택 사항)
              - [ ] 커스텀 아이콘 이미지 사용 또는 기본 아이콘 색상 변경
            - [ ] 마커 생성 함수 리팩토링 (`createMarker` 함수)
              - [ ] 관광 타입에 따라 색상 결정
              - [ ] 마커 옵션에 색상 적용
          - [ ] 마커 업데이트 로직
            - [ ] 필터 변경 시 마커 색상 업데이트
            - [ ] 관광 타입 필터 적용 시 해당 타입 마커만 표시 (이미 구현됨)
        - [ ] 마커 범례(Legend) 추가 (선택 사항)
          - [ ] `components/map-legend.tsx` 생성
            - [ ] 지도 우측 상단에 관광 타입별 색상 범례 표시
            - [ ] 각 관광 타입별 색상 박스 및 레이블 표시
            - [ ] 범례 클릭 시 해당 타입 마커만 표시 (필터링)
            - [ ] 범례 토글 버튼 (접기/펼치기)
            - [ ] 반응형 디자인 (모바일: 하단 고정, 데스크톱: 우측 상단)
          - [ ] `components/naver-map.tsx`에 범례 통합
            - [ ] 범례 컴포넌트를 지도 컨테이너 내부에 배치
            - [ ] 범례 클릭 시 필터 업데이트 (URL 파라미터 변경)
            - [ ] 현재 필터 상태에 따라 범례 항목 활성화 표시
        - [ ] UI/UX 개선
          - [ ] 마커 색상 대비 확인 (가독성)
          - [ ] 다크 모드 지원 (마커 색상 조정)
          - [ ] 마커 호버 시 색상 강조 (선택 사항)
        - [ ] 성능 최적화
          - [ ] 마커 아이콘 이미지 캐싱
          - [ ] 동일 색상 마커 재사용
    - [ ] 리스트 항목 호버 시 마커 강조 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] 호버 상태 관리 전략 정의
          - [ ] URL 파라미터 사용 vs 로컬 상태 사용 결정
          - [ ] URL 파라미터 사용 시: 브라우저 뒤로가기 지원, 공유 가능
          - [ ] 로컬 상태 사용 시: 빠른 반응, URL 변경 없음
          - [ ] 권장: 로컬 상태 사용 (호버는 일시적이므로)
        - [ ] TourCard 컴포넌트 수정
          - [ ] `components/tour-card.tsx`에 호버 이벤트 추가
            - [ ] `onMouseEnter` 이벤트 핸들러 추가
            - [ ] 호버 시 `onHover` prop 호출 (contentId 전달)
            - [ ] `onMouseLeave` 이벤트 핸들러 추가
            - [ ] 호버 시 `onLeave` prop 호출 (호버 해제)
            - [ ] Props에 `onHover`, `onLeave` 추가 (선택 사항)
        - [ ] TourList 컴포넌트 수정
          - [ ] `components/tour-list.tsx`에 호버 이벤트 전달
            - [ ] `onTourHover` prop 추가 (contentId를 받는 함수)
            - [ ] `onTourLeave` prop 추가 (호버 해제 함수)
            - [ ] TourCard에 호버 이벤트 핸들러 전달
        - [ ] TourListWithMap 컴포넌트 수정
          - [ ] `components/tour-list-with-map.tsx`에 호버 상태 관리 추가
            - [ ] 호버된 관광지 ID 상태 관리 (`hoveredTourId`, useState)
            - [ ] `handleTourHover` 함수 구현 (contentId를 받아서 상태 업데이트)
            - [ ] `handleTourLeave` 함수 구현 (호버 해제 시 상태 초기화)
            - [ ] TourList에 `onTourHover`, `onTourLeave` 전달
            - [ ] NaverMap에 `hoveredTourId` prop 전달
        - [ ] NaverMap 컴포넌트 수정
          - [ ] `components/naver-map.tsx`에 호버 강조 기능 추가
            - [ ] `hoveredTourId` prop 추가
            - [ ] 호버된 마커 식별 및 스타일 변경
            - [ ] 마커 크기 증가 (예: scale 1.2)
            - [ ] 마커 색상 변경 또는 테두리 추가
            - [ ] 호버된 마커의 z-index 증가 (다른 마커 위에 표시)
            - [ ] 호버된 마커의 인포윈도우 미리보기 표시 (선택 사항)
              - [ ] 호버 시 인포윈도우 자동 열기
              - [ ] 호버 해제 시 인포윈도우 자동 닫기
        - [ ] 성능 최적화
          - [ ] 호버 이벤트 디바운싱 (빠른 마우스 이동 시 불필요한 업데이트 방지)
          - [ ] 마커 스타일 변경 최적화 (CSS transition 사용)
        - [ ] UI/UX 개선
          - [ ] 호버 애니메이션 효과 (부드러운 전환)
          - [ ] 모바일에서는 호버 대신 탭/클릭으로 처리 (터치 디바이스)
          - [ ] 호버 시 카드에도 시각적 피드백 (배경색 변경 등)
        - [ ] 접근성 개선
          - [ ] 키보드 포커스 시에도 마커 강조 (Tab 키 네비게이션)
          - [ ] 스크린 리더를 위한 상태 알림
    - [ ] 현재 위치로 이동 버튼 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] 현재 위치 버튼 UI 구현
          - [ ] `components/naver-map.tsx`에 현재 위치 버튼 추가
            - [ ] 지도 우측 하단에 고정 버튼 배치
            - [ ] 위치 아이콘 사용 (MapPin 또는 Navigation 아이콘)
            - [ ] 버튼 스타일: 원형 버튼, 그림자 효과
            - [ ] 반응형 디자인 (모바일/데스크톱)
          - [ ] 버튼 상태 관리
            - [ ] 로딩 상태: 위치 조회 중 스피너 표시
            - [ ] 활성 상태: 현재 위치로 이동한 경우 버튼 강조
            - [ ] 비활성 상태: 위치 권한 거부 시 버튼 비활성화
        - [ ] Geolocation API 구현
          - [ ] 현재 위치 조회 함수 구현
            - [ ] `navigator.geolocation.getCurrentPosition()` 사용
            - [ ] 위치 정확도 옵션 설정 (`enableHighAccuracy: false` - 배터리 절약)
            - [ ] 타임아웃 설정 (10초)
            - [ ] 에러 처리 (PERMISSION_DENIED, POSITION_UNAVAILABLE, TIMEOUT)
          - [ ] 현재 위치 조회 시 로딩 상태 표시
            - [ ] 버튼에 로딩 스피너 표시
            - [ ] Toast 알림 표시 (선택 사항)
          - [ ] 현재 위치 조회 실패 시 에러 메시지 표시
            - [ ] 권한 거부: "위치 권한이 필요합니다. 브라우저 설정에서 위치 권한을 허용해주세요."
            - [ ] 위치 사용 불가: "현재 위치를 가져올 수 없습니다."
            - [ ] 타임아웃: "위치 조회 시간이 초과되었습니다."
            - [ ] Toast 알림 또는 Alert 컴포넌트로 표시
          - [ ] 현재 위치로 지도 중심 이동 및 줌 조정
            - [ ] 조회된 좌표로 지도 중심 이동 (`map.setCenter()`)
            - [ ] 적절한 줌 레벨 설정 (예: 15)
            - [ ] 부드러운 애니메이션 효과 (`map.panTo()` 사용)
        - [ ] 현재 위치 마커 표시 (선택 사항)
          - [ ] 사용자 현재 위치에 마커 표시
            - [ ] 다른 마커와 구분되는 커스텀 마커 사용
            - [ ] 파란색 원형 마커 또는 위치 아이콘 사용
            - [ ] 마커 크기: 관광지 마커보다 약간 크게
          - [ ] 현재 위치 마커 스타일
            - [ ] 파란색 원형 마커 (Naver Maps 기본 스타일)
            - [ ] 또는 커스텀 아이콘 이미지 사용
            - [ ] 애니메이션 효과 (펄스 효과, 선택 사항)
        - [ ] 권한 요청 처리
          - [ ] 위치 권한 요청 시 사용자 친화적 메시지 표시
            - [ ] 버튼 클릭 시 권한 요청 다이얼로그 표시
            - [ ] 권한 요청 이유 설명 (선택 사항)
          - [ ] 권한 거부 시 안내 메시지 표시
            - [ ] Alert 또는 Toast로 안내 메시지 표시
            - [ ] 브라우저 설정으로 이동하는 방법 안내 (선택 사항)
          - [ ] 권한 상태 확인
            - [ ] `navigator.permissions.query({ name: 'geolocation' })` 사용 (지원되는 경우)
            - [ ] 권한 상태에 따라 버튼 활성화/비활성화
        - [ ] 성능 최적화
          - [ ] 현재 위치 조회 결과 캐싱 (일정 시간 동안 재사용)
          - [ ] 위치 조회 중복 요청 방지
        - [ ] 접근성 개선
          - [ ] 버튼에 `aria-label` 추가 ("현재 위치로 이동")
          - [ ] 키보드 접근성 지원 (Enter/Space 키로 클릭)
          - [ ] 로딩 상태를 스크린 리더에 알림
    - [ ] 페이지 번호 선택 방식 추가 (선택 사항)
      - ---
      - 추가 개발 사항
        - [ ] Pagination 컴포넌트 구현
          - [ ] `components/pagination.tsx` 생성
            - [ ] Props: `currentPage`, `totalPages`, `onPageChange`, `className` (선택 사항)
            - [ ] 페이지 번호 버튼 표시 (1, 2, 3, ...)
            - [ ] 이전/다음 페이지 버튼
            - [ ] 첫 페이지/마지막 페이지 버튼 (선택 사항)
            - [ ] 현재 페이지 강조 표시 (다른 색상 또는 배경색)
            - [ ] 페이지 번호 클릭 시 `onPageChange` 호출
            - [ ] 비활성화 상태 처리 (첫 페이지에서 이전 버튼, 마지막 페이지에서 다음 버튼)
          - [ ] 페이지 번호 생략 로직 구현
            - [ ] 페이지 번호가 많을 경우 생략 표시 (예: 1 ... 5 6 7 ... 20)
            - [ ] 현재 페이지 주변 페이지만 표시 (예: 현재 페이지 ± 2)
            - [ ] 첫 페이지와 마지막 페이지는 항상 표시
            - [ ] 생략 표시: "..." 텍스트 또는 아이콘
          - [ ] 반응형 디자인
            - [ ] 모바일: 간소화된 버튼 (이전/다음만 또는 현재 페이지 ± 1)
            - [ ] 데스크톱: 전체 페이지 번호 표시
            - [ ] 태블릿: 중간 형태
        - [ ] 페이지네이션 모드 선택 기능
          - [ ] 필터 영역에 페이지네이션 모드 토글 추가
            - [ ] `components/tour-filters.tsx`에 페이지네이션 모드 선택 UI 추가
            - [ ] Radio Group 또는 Toggle Group 사용
            - [ ] 옵션: "무한 스크롤" / "페이지 번호"
            - [ ] 기본값: "무한 스크롤" (기존 동작 유지)
          - [ ] URL 파라미터 관리
            - [ ] `paginationMode` 파라미터 추가 ("infinite" | "numbered")
            - [ ] `lib/utils/filters.ts`에 `paginationMode` 필드 추가
            - [ ] 필터 변경 시 페이지네이션 모드 유지
          - [ ] 선택된 모드에 따라 페이지네이션 UI 표시/숨김
            - [ ] 무한 스크롤 모드: `InfiniteScrollTrigger` 표시
            - [ ] 페이지 번호 모드: `Pagination` 컴포넌트 표시
        - [ ] 페이지 번호 모드 로직 구현
          - [ ] `app/page.tsx`에 페이지 번호 모드 지원 추가
            - [ ] `paginationMode` 파라미터 읽기
            - [ ] 무한 스크롤 모드: 기존 로직 유지 (useInfiniteTours Hook 사용)
            - [ ] 페이지 번호 모드: `pageNo` 파라미터 기반 페이지네이션
              - [ ] `pageNo` 파라미터 읽기 (기본값: 1)
              - [ ] 해당 페이지의 데이터만 로드
              - [ ] `totalCount`와 `numOfRows`로 총 페이지 수 계산
          - [ ] `components/tour-list-with-map.tsx`에 페이지네이션 모드 지원 추가
            - [ ] `paginationMode` prop 추가
            - [ ] 무한 스크롤 모드: `useInfiniteTours` Hook 사용, `InfiniteScrollTrigger` 표시
            - [ ] 페이지 번호 모드: `Pagination` 컴포넌트 표시
            - [ ] 페이지 변경 핸들러 구현 (`handlePageChange`)
              - [ ] URL 파라미터에 `pageNo` 업데이트
              - [ ] 필터 파라미터는 유지
        - [ ] 페이지 이동 로직
          - [ ] 페이지 번호 클릭 시 해당 페이지로 이동
            - [ ] URL 파라미터에 `pageNo` 추가/업데이트
            - [ ] `router.push()`로 페이지 이동
          - [ ] 페이지 이동 시 스크롤 상단으로 이동
            - [ ] `window.scrollTo({ top: 0, behavior: 'smooth' })` 사용
            - [ ] 또는 목록 컨테이너로 스크롤 이동
          - [ ] 필터 변경 시 첫 페이지로 리셋
            - [ ] 필터 변경 핸들러에서 `pageNo`를 1로 리셋
            - [ ] 페이지네이션 모드는 유지
        - [ ] UI/UX 개선
          - [ ] 페이지 번호 버튼 스타일링
            - [ ] 현재 페이지: Primary 색상 배경
            - [ ] 다른 페이지: 기본 스타일
            - [ ] 호버 효과 추가
            - [ ] 비활성화 버튼 스타일 (이전/다음 버튼)
          - [ ] 로딩 상태 표시
            - [ ] 페이지 변경 시 로딩 인디케이터 표시
            - [ ] 기존 목록은 유지하거나 스켈레톤 UI 표시
          - [ ] 접근성 개선
            - [ ] 페이지 번호 버튼에 `aria-label` 추가
            - [ ] 현재 페이지에 `aria-current="page"` 추가
            - [ ] 키보드 네비게이션 지원
        - [ ] 성능 최적화
          - [ ] 페이지 변경 시 불필요한 리렌더링 방지
          - [ ] 페이지 번호 계산 최적화 (useMemo 사용)

## Phase 3: 상세페이지 (`/places/[contentId]`)

- [ ] 페이지 기본 구조
  - [ ] `app/places/[contentId]/page.tsx` 생성
    - [ ] 동적 라우팅 설정
    - [ ] 뒤로가기 버튼 (헤더)
    - [ ] 기본 레이아웃 구조
    - [ ] 라우팅 테스트
- [ ] 기본 정보 섹션 (MVP 2.4.1)
  - [ ] `components/tour-detail/detail-info.tsx` 생성
    - [ ] `getDetailCommon()` API 연동
    - [ ] 관광지명 (대제목)
    - [ ] 대표 이미지 (크게 표시)
    - [ ] 주소 표시 및 복사 기능
      - [ ] 클립보드 API 사용
      - [ ] 복사 완료 토스트
    - [ ] 전화번호 (클릭 시 전화 연결)
    - [ ] 홈페이지 (링크)
    - [ ] 개요 (긴 설명문)
    - [ ] 관광 타입 및 카테고리 뱃지
    - [ ] 정보 없는 항목 숨김 처리
- [ ] 운영 정보 섹션 (MVP 2.4.2)
  - [ ] `components/tour-detail/detail-intro.tsx` 생성
    - [ ] `getDetailIntro()` API 연동
    - [ ] 운영시간/개장시간
    - [ ] 휴무일
    - [ ] 이용요금
    - [ ] 주차 가능 여부
    - [ ] 수용인원
    - [ ] 체험 프로그램
    - [ ] 유모차/반려동물 동반 가능 여부
    - [ ] 정보 없는 항목 숨김 처리
- [ ] 이미지 갤러리 (MVP 2.4.3)
  - [ ] `components/tour-detail/detail-gallery.tsx` 생성
    - [ ] `getDetailImage()` API 연동
    - [ ] 대표 이미지 + 서브 이미지들
    - [ ] 이미지 슬라이드 기능 (Swiper 또는 캐러셀)
    - [ ] 이미지 클릭 시 전체화면 모달
    - [ ] 이미지 없으면 기본 이미지
    - [ ] Next.js Image 컴포넌트 사용 (최적화)
- [ ] 지도 섹션 (MVP 2.4.4)
  - [ ] `components/tour-detail/detail-map.tsx` 생성
    - [ ] 해당 관광지 위치 표시
    - [ ] 마커 1개 표시
    - [ ] "길찾기" 버튼
      - [ ] 네이버 지도 앱/웹 연동
      - [ ] URL: `https://map.naver.com/v5/directions/{좌표}`
    - [ ] 좌표 정보 표시 (선택 사항)
- [ ] 공유 기능 (MVP 2.4.5)
  - [ ] `components/tour-detail/share-button.tsx` 생성
    - [ ] URL 복사 기능
      - [ ] `navigator.clipboard.writeText()` 사용
      - [ ] HTTPS 환경 확인
    - [ ] 복사 완료 토스트 메시지
    - [ ] 공유 아이콘 버튼 (Share/Link 아이콘)
  - [ ] Open Graph 메타태그
    - [ ] `app/places/[contentId]/page.tsx`에 Metadata 생성
    - [ ] `og:title` - 관광지명
    - [ ] `og:description` - 관광지 설명 (100자 이내)
    - [ ] `og:image` - 대표 이미지 (1200x630 권장)
    - [ ] `og:url` - 상세페이지 URL
    - [ ] `og:type` - "website"
- [ ] 북마크 기능 (MVP 2.4.5)
  - [ ] `components/bookmarks/bookmark-button.tsx` 생성
    - [ ] 별 아이콘 (채워짐/비어있음)
    - [ ] 북마크 상태 확인 (Supabase 조회)
    - [ ] 북마크 추가/제거 기능
    - [ ] 인증된 사용자 확인 (Clerk)
    - [ ] 로그인하지 않은 경우: 로그인 유도 또는 localStorage 임시 저장
  - [ ] Supabase 연동
    - [ ] `lib/api/supabase-api.ts` 생성
      - [ ] `getBookmark()` - 북마크 조회
      - [ ] `addBookmark()` - 북마크 추가
      - [ ] `removeBookmark()` - 북마크 제거
      - [ ] `getUserBookmarks()` - 사용자 북마크 목록
    - [ ] `bookmarks` 테이블 사용 (db.sql 참고)
      - [ ] `user_id` (users 테이블 참조)
      - [ ] `content_id` (한국관광공사 API contentid)
      - [ ] UNIQUE 제약 (user_id, content_id)
  - [ ] 상세페이지에 북마크 버튼 추가
- [ ] 반려동물 정보 섹션 (MVP 2.5)
  - [ ] `components/tour-detail/detail-pet-tour.tsx` 생성
    - [ ] `getDetailPetTour()` API 연동
    - [ ] 반려동물 동반 가능 여부 표시
    - [ ] 반려동물 크기 제한 정보
    - [ ] 반려동물 입장 가능 장소 (실내/실외)
    - [ ] 반려동물 동반 추가 요금
    - [ ] 반려동물 전용 시설 정보
    - [ ] 아이콘 및 뱃지 디자인 (🐾)
    - [ ] 주의사항 강조 표시
- [ ] 추천 관광지 섹션 (선택 사항)
  - [ ] 같은 지역 또는 타입의 다른 관광지 추천
  - [ ] 카드 형태로 표시
- [ ] 최종 통합 및 스타일링
  - [ ] 모든 섹션 통합
  - [ ] 반응형 디자인 확인
  - [ ] 모바일 최적화
  - [ ] 접근성 확인 (ARIA 라벨, 키보드 네비게이션)

## Phase 4: 통계 대시보드 페이지 (`/stats`)

- [ ] 페이지 기본 구조
  - [ ] `app/stats/page.tsx` 생성
    - [ ] 기본 레이아웃 구조
    - [ ] 반응형 레이아웃 설정 (모바일 우선)
    - [ ] Server Component로 구현
- [ ] 통계 데이터 수집
  - [ ] `lib/api/stats-api.ts` 생성
    - [ ] `getRegionStats()` - 지역별 관광지 개수 집계
      - [ ] `areaBasedList2` API로 각 지역별 totalCount 조회
      - [ ] 지역 코드별로 API 호출
    - [ ] `getTypeStats()` - 타입별 관광지 개수 집계
      - [ ] `areaBasedList2` API로 각 타입별 totalCount 조회
      - [ ] contentTypeId별로 API 호출
    - [ ] `getStatsSummary()` - 전체 통계 요약
      - [ ] 전체 관광지 수
      - [ ] Top 3 지역
      - [ ] Top 3 타입
      - [ ] 마지막 업데이트 시간
    - [ ] 병렬 API 호출로 성능 최적화
    - [ ] 에러 처리 및 재시도 로직
    - [ ] 데이터 캐싱 (revalidate: 3600)
- [ ] 통계 요약 카드
  - [ ] `components/stats/stats-summary.tsx` 생성
    - [ ] 전체 관광지 수 표시
    - [ ] Top 3 지역 표시 (카드 형태)
    - [ ] Top 3 타입 표시 (카드 형태)
    - [ ] 마지막 업데이트 시간 표시
    - [ ] 로딩 상태 (Skeleton UI)
    - [ ] 카드 레이아웃 디자인
- [ ] 지역별 분포 차트 (Bar Chart)
  - [ ] `components/stats/region-chart.tsx` 생성
    - [ ] shadcn/ui Chart 컴포넌트 설치 (Bar)
    - [ ] recharts 기반 Bar Chart 구현
    - [ ] X축: 지역명 (서울, 부산, 제주 등)
    - [ ] Y축: 관광지 개수
    - [ ] 상위 10개 지역 표시 (또는 전체)
    - [ ] 바 클릭 시 해당 지역 목록 페이지로 이동
    - [ ] 호버 시 정확한 개수 표시
    - [ ] 다크/라이트 모드 지원
    - [ ] 반응형 디자인
    - [ ] 로딩 상태
    - [ ] 접근성 (ARIA 라벨, 키보드 네비게이션)
- [ ] 타입별 분포 차트 (Donut Chart)
  - [ ] `components/stats/type-chart.tsx` 생성
    - [ ] shadcn/ui Chart 컴포넌트 설치 (Pie/Donut)
    - [ ] recharts 기반 Donut Chart 구현
    - [ ] 타입별 비율 (백분율)
    - [ ] 타입별 개수 표시
    - [ ] 섹션 클릭 시 해당 타입 목록 페이지로 이동
    - [ ] 호버 시 타입명, 개수, 비율 표시
    - [ ] 다크/라이트 모드 지원
    - [ ] 반응형 디자인
    - [ ] 로딩 상태
    - [ ] 접근성 (ARIA 라벨)
- [ ] 페이지 통합
  - [ ] `app/stats/page.tsx`에 모든 컴포넌트 통합
    - [ ] 통계 요약 카드 (상단)
    - [ ] 지역별 분포 차트 (중단)
    - [ ] 타입별 분포 차트 (하단)
  - [ ] 에러 처리 (에러 메시지 + 재시도 버튼)
  - [ ] 네비게이션에 통계 페이지 링크 추가
  - [ ] 최종 페이지 확인

## Phase 5: 북마크 페이지 (`/bookmarks`) - 선택 사항

- [ ] Supabase 설정 확인
  - [ ] `bookmarks` 테이블 확인 (db.sql 참고)
    - [ ] `users` 테이블과의 관계 확인
    - [ ] 인덱스 확인 (user_id, content_id, created_at)
    - [ ] RLS 비활성화 확인 (개발 환경)
- [ ] 북마크 목록 페이지
  - [ ] `app/bookmarks/page.tsx` 생성
    - [ ] 인증된 사용자만 접근 가능
    - [ ] 로그인하지 않은 경우 로그인 유도
  - [ ] `components/bookmarks/bookmark-list.tsx` 생성
    - [ ] 사용자 북마크 목록 조회 (`getUserBookmarks()`)
    - [ ] 카드 레이아웃 (홈페이지와 동일한 tour-card 사용)
    - [ ] 빈 상태 처리 (북마크 없을 때)
    - [ ] 로딩 상태 (Skeleton UI)
- [ ] 북마크 관리 기능
  - [ ] 정렬 옵션
    - [ ] 최신순 (created_at DESC)
    - [ ] 이름순 (가나다순)
    - [ ] 지역별
  - [ ] 일괄 삭제 기능
    - [ ] 체크박스 선택
    - [ ] 선택 항목 삭제
    - [ ] 확인 다이얼로그
  - [ ] 개별 삭제 기능
    - [ ] 각 카드에 삭제 버튼
- [ ] 페이지 통합 및 스타일링
  - [ ] 반응형 디자인 확인
  - [ ] 최종 페이지 확인

## Phase 6: 최적화 & 배포

- [ ] 이미지 최적화
  - [ ] `next.config.ts` 외부 도메인 설정
    - [ ] 한국관광공사 이미지 도메인 추가
    - [ ] 네이버 지도 이미지 도메인 추가
  - [ ] Next.js Image 컴포넌트 사용 확인
    - [ ] priority 속성 (above-the-fold)
    - [ ] lazy loading (below-the-fold)
    - [ ] responsive sizes 설정
- [ ] 전역 에러 핸들링
  - [ ] `app/error.tsx` 생성
  - [ ] `app/global-error.tsx` 생성
  - [ ] API 에러 처리 개선
- [ ] 404 페이지
  - [ ] `app/not-found.tsx` 생성
    - [ ] 사용자 친화적인 메시지
    - [ ] 홈으로 돌아가기 버튼
- [ ] SEO 최적화
  - [ ] 메타태그 설정 (`app/layout.tsx`)
    - [ ] 기본 title, description
    - [ ] Open Graph 태그
    - [ ] Twitter Card 태그
  - [ ] `app/sitemap.ts` 생성
    - [ ] 동적 sitemap 생성 (관광지 상세페이지 포함)
  - [ ] `app/robots.ts` 생성
- [ ] 성능 최적화
  - [ ] Lighthouse 점수 측정 (목표: > 80)
  - [ ] 코드 분할 확인
  - [ ] 불필요한 번들 제거
  - [ ] API 응답 캐싱 전략 확인
- [ ] 환경변수 보안 검증
  - [ ] 모든 필수 환경변수 확인
  - [ ] `.env.example` 업데이트
  - [ ] 프로덕션 환경변수 설정 가이드 작성
- [ ] 배포 준비
  - [ ] Vercel 배포 설정
  - [ ] 환경변수 설정 (Vercel 대시보드)
  - [ ] 빌드 테스트 (`pnpm build`)
  - [ ] 프로덕션 배포 및 테스트

## 추가 작업 (선택 사항)

- [ ] 다크 모드 지원
  - [ ] 테마 전환 기능
  - [ ] 모든 컴포넌트 다크 모드 스타일 적용
- [ ] PWA 지원
  - [ ] `app/manifest.ts` 생성
  - [ ] Service Worker 설정
  - [ ] 오프라인 지원
- [ ] 접근성 개선
  - [ ] ARIA 라벨 추가
  - [ ] 키보드 네비게이션 개선
  - [ ] 색상 대비 확인 (WCAG AA)
- [ ] 성능 모니터링
  - [ ] Web Vitals 측정
  - [ ] 에러 로깅 (Sentry 등)
- [ ] 사용자 피드백
  - [ ] 피드백 수집 기능
  - [ ] 버그 리포트 기능
